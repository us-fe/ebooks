import{_ as n,d as e}from"./app.a957722f.js";const s={},a=e(`<h1 id="you-don-t-know-js-yet-scope-closures-2nd-edition" tabindex="-1"><a class="header-anchor" href="#you-don-t-know-js-yet-scope-closures-2nd-edition" aria-hidden="true">#</a> You Don&#39;t Know JS Yet: Scope &amp; Closures - 2nd Edition</h1><h1 id="chapter-5-the-not-so-secret-lifecycle-of-variables" tabindex="-1"><a class="header-anchor" href="#chapter-5-the-not-so-secret-lifecycle-of-variables" aria-hidden="true">#</a> Chapter 5: The (Not So) Secret Lifecycle of Variables</h1><p>By now you should have a decent grasp of the nesting of scopes, from the global scope downward\u2014called a program&#39;s scope chain.</p><p>But just knowing which scope a variable comes from is only part of the story. If a variable declaration appears past the first statement of a scope, how will any references to that identifier <em>before</em> the declaration behave? What happens if you try to declare the same variable twice in a scope?</p><p>JS&#39;s particular flavor of lexical scope is rich with nuance in how and when variables come into existence and become available to the program.</p><h2 id="when-can-i-use-a-variable" tabindex="-1"><a class="header-anchor" href="#when-can-i-use-a-variable" aria-hidden="true">#</a> When Can I Use a Variable?</h2><p>At what point does a variable become available to use within its scope? There may seem to be an obvious answer: <em>after</em> the variable has been declared/created. Right? Not quite.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token function">greeting</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Hello!</span>

<span class="token keyword">function</span> <span class="token function">greeting</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">&quot;Hello!&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>This code works fine. You may have seen or even written code like it before. But did you ever wonder how or why it works? Specifically, why can you access the identifier <code>greeting</code> from line 1 (to retrieve and execute a function reference), even though the <code>greeting()</code> function declaration doesn&#39;t occur until line 4?</p><p>Recall Chapter 1 points out that all identifiers are registered to their respective scopes during compile time. Moreover, every identifier is <em>created</em> at the beginning of the scope it belongs to, <strong>every time that scope is entered</strong>.</p><p>The term most commonly used for a variable being visible from the beginning of its enclosing scope, even though its declaration may appear further down in the scope, is called <strong>hoisting</strong>.</p><p>But hoisting alone doesn&#39;t fully answer the question. We can see an identifier called <code>greeting</code> from the beginning of the scope, but why can we <strong>call</strong> the <code>greeting()</code> function before it&#39;s been declared?</p><p>In other words, how does the variable <code>greeting</code> have any value (the function reference) assigned to it, from the moment the scope starts running? The answer is a special characteristic of formal <code>function</code> declarations, called <em>function hoisting</em>. When a <code>function</code> declaration&#39;s name identifier is registered at the top of its scope, it&#39;s additionally auto-initialized to that function&#39;s reference. That&#39;s why the function can be called throughout the entire scope!</p><p>One key detail is that both <em>function hoisting</em> and <code>var</code>-flavored <em>variable hoisting</em> attach their name identifiers to the nearest enclosing <strong>function scope</strong> (or, if none, the global scope), not a block scope.</p><table><thead><tr><th style="text-align:left;">NOTE:</th></tr></thead><tbody><tr><td style="text-align:left;">Declarations with <code>let</code> and <code>const</code> still hoist (see the TDZ discussion later in this chapter). But these two declaration forms attach to their enclosing block rather than just an enclosing function as with <code>var</code> and <code>function</code> declarations. See &quot;Scoping with Blocks&quot; in Chapter 6 for more information.</td></tr></tbody></table><h3 id="hoisting-declaration-vs-expression" tabindex="-1"><a class="header-anchor" href="#hoisting-declaration-vs-expression" aria-hidden="true">#</a> Hoisting: Declaration vs. Expression</h3><p><em>Function hoisting</em> only applies to formal <code>function</code> declarations (specifically those which appear outside of blocks\u2014see &quot;FiB&quot; in Chapter 6), not to <code>function</code> expression assignments. Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token function">greeting</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// TypeError</span>

<span class="token keyword">var</span> <span class="token function-variable function">greeting</span> <span class="token operator">=</span> <span class="token keyword">function</span> <span class="token function">greeting</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">&quot;Hello!&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>Line 1 (<code>greeting();</code>) throws an error. But the <em>kind</em> of error thrown is very important to notice. A <code>TypeError</code> means we&#39;re trying to do something with a value that is not allowed. Depending on your JS environment, the error message would say something like, &quot;&#39;undefined&#39; is not a function,&quot; or more helpfully, &quot;&#39;greeting&#39; is not a function.&quot;</p><p>Notice that the error is <strong>not</strong> a <code>ReferenceError</code>. JS isn&#39;t telling us that it couldn&#39;t find <code>greeting</code> as an identifier in the scope. It&#39;s telling us that <code>greeting</code> was found but doesn&#39;t hold a function reference at that moment. Only functions can be invoked, so attempting to invoke some non-function value results in an error.</p><p>But what does <code>greeting</code> hold, if not the function reference?</p><p>In addition to being hoisted, variables declared with <code>var</code> are also automatically initialized to <code>undefined</code> at the beginning of their scope\u2014again, the nearest enclosing function, or the global. Once initialized, they&#39;re available to be used (assigned to, retrieved from, etc.) throughout the whole scope.</p><p>So on that first line, <code>greeting</code> exists, but it holds only the default <code>undefined</code> value. It&#39;s not until line 4 that <code>greeting</code> gets assigned the function reference.</p><p>Pay close attention to the distinction here. A <code>function</code> declaration is hoisted <strong>and initialized to its function value</strong> (again, called <em>function hoisting</em>). A <code>var</code> variable is also hoisted, and then auto-initialized to <code>undefined</code>. Any subsequent <code>function</code> expression assignments to that variable don&#39;t happen until that assignment is processed during runtime execution.</p><p>In both cases, the name of the identifier is hoisted. But the function reference association isn&#39;t handled at initialization time (beginning of the scope) unless the identifier was created in a formal <code>function</code> declaration.</p><h3 id="variable-hoisting" tabindex="-1"><a class="header-anchor" href="#variable-hoisting" aria-hidden="true">#</a> Variable Hoisting</h3><p>Let&#39;s look at another example of <em>variable hoisting</em>:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code>greeting <span class="token operator">=</span> <span class="token string">&quot;Hello!&quot;</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>greeting<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Hello!</span>

<span class="token keyword">var</span> greeting <span class="token operator">=</span> <span class="token string">&quot;Howdy!&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Though <code>greeting</code> isn&#39;t declared until line 5, it&#39;s available to be assigned to as early as line 1. Why?</p><p>There&#39;s two necessary parts to the explanation:</p><ul><li>the identifier is hoisted,</li><li><strong>and</strong> it&#39;s automatically initialized to the value <code>undefined</code> from the top of the scope.</li></ul><table><thead><tr><th style="text-align:left;">NOTE:</th></tr></thead><tbody><tr><td style="text-align:left;">Using <em>variable hoisting</em> of this sort probably feels unnatural, and many readers might rightly want to avoid relying on it in their programs. But should all hoisting (including <em>function hoisting</em>) be avoided? We&#39;ll explore these different perspectives on hoisting in more detail in Appendix A.</td></tr></tbody></table><h2 id="hoisting-yet-another-metaphor" tabindex="-1"><a class="header-anchor" href="#hoisting-yet-another-metaphor" aria-hidden="true">#</a> Hoisting: Yet Another Metaphor</h2><p>Chapter 2 was full of metaphors (to illustrate scope), but here we are faced with yet another: hoisting itself. Rather than hoisting being a concrete execution step the JS engine performs, it&#39;s more useful to think of hoisting as a visualization of various actions JS takes in setting up the program <strong>before execution</strong>.</p><p>The typical assertion of what hoisting means: <em>lifting</em>\u2014like lifting a heavy weight upward\u2014any identifiers all the way to the top of a scope. The explanation often asserted is that the JS engine will actually <em>rewrite</em> that program before execution, so that it looks more like this:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> greeting<span class="token punctuation">;</span>           <span class="token comment">// hoisted declaration</span>
greeting <span class="token operator">=</span> <span class="token string">&quot;Hello!&quot;</span><span class="token punctuation">;</span>    <span class="token comment">// the original line 1</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>greeting<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// Hello!</span>
greeting <span class="token operator">=</span> <span class="token string">&quot;Howdy!&quot;</span><span class="token punctuation">;</span>    <span class="token comment">// \`var\` is gone!</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The hoisting (metaphor) proposes that JS pre-processes the original program and re-arranges it a bit, so that all the declarations have been moved to the top of their respective scopes, before execution. Moreover, the hoisting metaphor asserts that <code>function</code> declarations are, in their entirety, hoisted to the top of each scope. Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code>studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>
<span class="token function">greeting</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Hello Suzy!</span>

<span class="token keyword">function</span> <span class="token function">greeting</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token string">Hello </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span> studentName <span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">!</span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">var</span> studentName<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>The &quot;rule&quot; of the hoisting metaphor is that function declarations are hoisted first, then variables are hoisted immediately after all the functions. Thus, the hoisting story suggests that program is <em>re-arranged</em> by the JS engine to look like this:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">function</span> <span class="token function">greeting</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token string">Hello </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span> studentName <span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">!</span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">var</span> studentName<span class="token punctuation">;</span>

studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>
<span class="token function">greeting</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Hello Suzy!</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>This hoisting metaphor is convenient. Its benefit is allowing us to hand wave over the magical look-ahead pre-processing necessary to find all these declarations buried deep in scopes and somehow move (hoist) them to the top; we can just think about the program as if it&#39;s executed by the JS engine in a <strong>single pass</strong>, top-down.</p><p>Single-pass definitely seems more straightforward than Chapter 1&#39;s assertion of a two-phase processing.</p><p>Hoisting as a mechanism for re-ordering code may be an attractive simplification, but it&#39;s not accurate. The JS engine doesn&#39;t actually re-arrange the code. It can&#39;t magically look ahead and find declarations; the only way to accurately find them, as well as all the scope boundaries in the program, would be to fully parse the code.</p><p>Guess what parsing is? The first phase of the two-phase processing! There&#39;s no magical mental gymnastics that gets around that fact.</p><p>So if the hoisting metaphor is (at best) inaccurate, what should we do with the term? I think it&#39;s still useful\u2014indeed, even members of TC39 regularly use it!\u2014but I don&#39;t think we should claim it&#39;s an actual re-arrangement of source code.</p><table><thead><tr><th style="text-align:left;">WARNING:</th></tr></thead><tbody><tr><td style="text-align:left;">Incorrect or incomplete mental models often still seem sufficient because they can occasionally lead to accidental right answers. But in the long run it&#39;s harder to accurately analyze and predict outcomes if your thinking isn&#39;t particularly aligned with how the JS engine works.</td></tr></tbody></table><p>I assert that hoisting <em>should</em> be used to refer to the <strong>compile-time operation</strong> of generating runtime instructions for the automatic registration of a variable at the beginning of its scope, each time that scope is entered.</p><p>That&#39;s a subtle but important shift, from hoisting as a runtime behavior to its proper place among compile-time tasks.</p><h2 id="re-declaration" tabindex="-1"><a class="header-anchor" href="#re-declaration" aria-hidden="true">#</a> Re-declaration?</h2><p>What do you think happens when a variable is declared more than once in the same scope? Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Frank&quot;</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Frank</span>

<span class="token keyword">var</span> studentName<span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// ???</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>What do you expect to be printed for that second message? Many believe the second <code>var studentName</code> has re-declared the variable (and thus &quot;reset&quot; it), so they expect <code>undefined</code> to be printed.</p><p>But is there such a thing as a variable being &quot;re-declared&quot; in the same scope? No.</p><p>If you consider this program from the perspective of the hoisting metaphor, the code would be re-arranged like this for execution purposes:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> studentName<span class="token punctuation">;</span>
<span class="token keyword">var</span> studentName<span class="token punctuation">;</span>    <span class="token comment">// clearly a pointless no-op!</span>

studentName <span class="token operator">=</span> <span class="token string">&quot;Frank&quot;</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Frank</span>

console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Frank</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>Since hoisting is actually about registering a variable at the beginning of a scope, there&#39;s nothing to be done in the middle of the scope where the original program actually had the second <code>var studentName</code> statement. It&#39;s just a no-op(eration), a pointless statement.</p><table><thead><tr><th style="text-align:left;">TIP:</th></tr></thead><tbody><tr><td style="text-align:left;">In the style of the conversation narrative from Chapter 2, <em>Compiler</em> would find the second <code>var</code> declaration statement and ask the <em>Scope Manager</em> if it had already seen a <code>studentName</code> identifier; since it had, there wouldn&#39;t be anything else to do.</td></tr></tbody></table><p>It&#39;s also important to point out that <code>var studentName;</code> doesn&#39;t mean <code>var studentName = undefined;</code>, as most assume. Let&#39;s prove they&#39;re different by considering this variation of the program:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Frank&quot;</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// Frank</span>

<span class="token keyword">var</span> studentName<span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// Frank &lt;--- still!</span>

<span class="token comment">// let&#39;s add the initialization explicitly</span>
<span class="token keyword">var</span> studentName <span class="token operator">=</span> <span class="token keyword">undefined</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// undefined &lt;--- see!?</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>See how the explicit <code>= undefined</code> initialization produces a different outcome than assuming it happens implicitly when omitted? In the next section, we&#39;ll revisit this topic of initialization of variables from their declarations.</p><p>A repeated <code>var</code> declaration of the same identifier name in a scope is effectively a do-nothing operation. Here&#39;s another illustration, this time across a function of the same name:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> greeting<span class="token punctuation">;</span>

<span class="token keyword">function</span> <span class="token function">greeting</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">&quot;Hello!&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token comment">// basically, a no-op</span>
<span class="token keyword">var</span> greeting<span class="token punctuation">;</span>

<span class="token keyword">typeof</span> greeting<span class="token punctuation">;</span>        <span class="token comment">// &quot;function&quot;</span>

<span class="token keyword">var</span> greeting <span class="token operator">=</span> <span class="token string">&quot;Hello!&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">typeof</span> greeting<span class="token punctuation">;</span>        <span class="token comment">// &quot;string&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p>The first <code>greeting</code> declaration registers the identifier to the scope, and because it&#39;s a <code>var</code> the auto-initialization will be <code>undefined</code>. The <code>function</code> declaration doesn&#39;t need to re-register the identifier, but because of <em>function hoisting</em> it overrides the auto-initialization to use the function reference. The second <code>var greeting</code> by itself doesn&#39;t do anything since <code>greeting</code> is already an identifier and <em>function hoisting</em> already took precedence for the auto-initialization.</p><p>Actually assigning <code>&quot;Hello!&quot;</code> to <code>greeting</code> changes its value from the initial function <code>greeting()</code> to the string; <code>var</code> itself doesn&#39;t have any effect.</p><p>What about repeating a declaration within a scope using <code>let</code> or <code>const</code>?</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">let</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Frank&quot;</span><span class="token punctuation">;</span>

console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">let</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>This program will not execute, but instead immediately throw a <code>SyntaxError</code>. Depending on your JS environment, the error message will indicate something like: &quot;studentName has already been declared.&quot; In other words, this is a case where attempted &quot;re-declaration&quot; is explicitly not allowed!</p><p>It&#39;s not just that two declarations involving <code>let</code> will throw this error. If either declaration uses <code>let</code>, the other can be either <code>let</code> or <code>var</code>, and the error will still occur, as illustrated with these two variations:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Frank&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">let</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>and:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">let</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Frank&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>In both cases, a <code>SyntaxError</code> is thrown on the <em>second</em> declaration. In other words, the only way to &quot;re-declare&quot; a variable is to use <code>var</code> for all (two or more) of its declarations.</p><p>But why disallow it? The reason for the error is not technical per se, as <code>var</code> &quot;re-declaration&quot; has always been allowed; clearly, the same allowance could have been made for <code>let</code>.</p><p>It&#39;s really more of a &quot;social engineering&quot; issue. &quot;Re-declaration&quot; of variables is seen by some, including many on the TC39 body, as a bad habit that can lead to program bugs. So when ES6 introduced <code>let</code>, they decided to prevent &quot;re-declaration&quot; with an error.</p><table><thead><tr><th style="text-align:left;">NOTE:</th></tr></thead><tbody><tr><td style="text-align:left;">This is of course a stylistic opinion, not really a technical argument. Many developers agree with the position, and that&#39;s probably in part why TC39 included the error (as well as <code>let</code> conforming to <code>const</code>). But a reasonable case could have been made that staying consistent with <code>var</code>&#39;s precedent was more prudent, and that such opinion-enforcement was best left to opt-in tooling like linters. In Appendix A, we&#39;ll explore whether <code>var</code> (and its associated behavior, like &quot;re-declaration&quot;) can still be useful in modern JS.</td></tr></tbody></table><p>When <em>Compiler</em> asks <em>Scope Manager</em> about a declaration, if that identifier has already been declared, and if either/both declarations were made with <code>let</code>, an error is thrown. The intended signal to the developer is &quot;Stop relying on sloppy re-declaration!&quot;</p><h3 id="constants" tabindex="-1"><a class="header-anchor" href="#constants" aria-hidden="true">#</a> Constants?</h3><p>The <code>const</code> keyword is more constrained than <code>let</code>. Like <code>let</code>, <code>const</code> cannot be repeated with the same identifier in the same scope. But there&#39;s actually an overriding technical reason why that sort of &quot;re-declaration&quot; is disallowed, unlike <code>let</code> which disallows &quot;re-declaration&quot; mostly for stylistic reasons.</p><p>The <code>const</code> keyword requires a variable to be initialized, so omitting an assignment from the declaration results in a <code>SyntaxError</code>:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">const</span> empty<span class="token punctuation">;</span>   <span class="token comment">// SyntaxError</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>const</code> declarations create variables that cannot be re-assigned:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">const</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Frank&quot;</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Frank</span>

studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>   <span class="token comment">// TypeError</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>The <code>studentName</code> variable cannot be re-assigned because it&#39;s declared with a <code>const</code>.</p><table><thead><tr><th style="text-align:left;">WARNING:</th></tr></thead><tbody><tr><td style="text-align:left;">The error thrown when re-assigning <code>studentName</code> is a <code>TypeError</code>, not a <code>SyntaxError</code>. The subtle distinction here is actually pretty important, but unfortunately far too easy to miss. Syntax errors represent faults in the program that stop it from even starting execution. Type errors represent faults that arise during program execution. In the preceding snippet, <code>&quot;Frank&quot;</code> is printed out before we process the re-assignment of <code>studentName</code>, which then throws the error.</td></tr></tbody></table><p>So if <code>const</code> declarations cannot be re-assigned, and <code>const</code> declarations always require assignments, then we have a clear technical reason why <code>const</code> must disallow any &quot;re-declarations&quot;: any <code>const</code> &quot;re-declaration&quot; would also necessarily be a <code>const</code> re-assignment, which can&#39;t be allowed!</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">const</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Frank&quot;</span><span class="token punctuation">;</span>

<span class="token comment">// obviously this must be an error</span>
<span class="token keyword">const</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Since <code>const</code> &quot;re-declaration&quot; must be disallowed (on those technical grounds), TC39 essentially felt that <code>let</code> &quot;re-declaration&quot; should be disallowed as well, for consistency. It&#39;s debatable if this was the best choice, but at least we have the reasoning behind the decision.</p><h3 id="loops" tabindex="-1"><a class="header-anchor" href="#loops" aria-hidden="true">#</a> Loops</h3><p>So it&#39;s clear from our previous discussion that JS doesn&#39;t really want us to &quot;re-declare&quot; our variables within the same scope. That probably seems like a straightforward admonition, until you consider what it means for repeated execution of declaration statements in loops. Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> keepGoing <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
<span class="token keyword">while</span> <span class="token punctuation">(</span>keepGoing<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> value <span class="token operator">=</span> Math<span class="token punctuation">.</span><span class="token function">random</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>value <span class="token operator">&gt;</span> <span class="token number">0.5</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        keepGoing <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Is <code>value</code> being &quot;re-declared&quot; repeatedly in this program? Will we get errors thrown? No.</p><p>All the rules of scope (including &quot;re-declaration&quot; of <code>let</code>-created variables) are applied <em>per scope instance</em>. In other words, each time a scope is entered during execution, everything resets.</p><p>Each loop iteration is its own new scope instance, and within each scope instance, <code>value</code> is only being declared once. So there&#39;s no attempted &quot;re-declaration,&quot; and thus no error. Before we consider other loop forms, what if the <code>value</code> declaration in the previous snippet were changed to a <code>var</code>?</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> keepGoing <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
<span class="token keyword">while</span> <span class="token punctuation">(</span>keepGoing<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">var</span> value <span class="token operator">=</span> Math<span class="token punctuation">.</span><span class="token function">random</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>value <span class="token operator">&gt;</span> <span class="token number">0.5</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        keepGoing <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Is <code>value</code> being &quot;re-declared&quot; here, especially since we know <code>var</code> allows it? No. Because <code>var</code> is not treated as a block-scoping declaration (see Chapter 6), it attaches itself to the global scope. So there&#39;s just one <code>value</code> variable, in the same scope as <code>keepGoing</code> (global scope, in this case). No &quot;re-declaration&quot; here, either!</p><p>One way to keep this all straight is to remember that <code>var</code>, <code>let</code>, and <code>const</code> keywords are effectively <em>removed</em> from the code by the time it starts to execute. They&#39;re handled entirely by the compiler.</p><p>If you mentally erase the declarator keywords and then try to process the code, it should help you decide if and when (re-)declarations might occur.</p><p>What about &quot;re-declaration&quot; with other loop forms, like <code>for</code>-loops?</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">let</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> <span class="token number">3</span><span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> value <span class="token operator">=</span> i <span class="token operator">*</span> <span class="token number">10</span><span class="token punctuation">;</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span> i <span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">: </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span> value <span class="token interpolation-punctuation punctuation">}</span></span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token comment">// 0: 0</span>
<span class="token comment">// 1: 10</span>
<span class="token comment">// 2: 20</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>It should be clear that there&#39;s only one <code>value</code> declared per scope instance. But what about <code>i</code>? Is it being &quot;re-declared&quot;?</p><p>To answer that, consider what scope <code>i</code> is in. It might seem like it would be in the outer (in this case, global) scope, but it&#39;s not. It&#39;s in the scope of <code>for</code>-loop body, just like <code>value</code> is. In fact, you could sorta think about that loop in this more verbose equivalent form:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token punctuation">{</span>
    <span class="token comment">// a fictional variable for illustration</span>
    <span class="token keyword">let</span> $$i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>

    <span class="token keyword">for</span> <span class="token punctuation">(</span> <span class="token comment">/* nothing */</span><span class="token punctuation">;</span> $$i <span class="token operator">&lt;</span> <span class="token number">3</span><span class="token punctuation">;</span> $$i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token comment">// here&#39;s our actual loop \`i\`!</span>
        <span class="token keyword">let</span> i <span class="token operator">=</span> $$i<span class="token punctuation">;</span>

        <span class="token keyword">let</span> value <span class="token operator">=</span> i <span class="token operator">*</span> <span class="token number">10</span><span class="token punctuation">;</span>
        console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span> i <span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">: </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span> value <span class="token interpolation-punctuation punctuation">}</span></span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
    <span class="token comment">// 0: 0</span>
    <span class="token comment">// 1: 10</span>
    <span class="token comment">// 2: 20</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br></div></div><p>Now it should be clear: the <code>i</code> and <code>value</code> variables are both declared exactly once <strong>per scope instance</strong>. No &quot;re-declaration&quot; here.</p><p>What about other <code>for</code>-loop forms?</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">let</span> index <span class="token keyword">in</span> students<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// this is fine</span>
<span class="token punctuation">}</span>

<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">let</span> student <span class="token keyword">of</span> students<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// so is this</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Same thing with <code>for..in</code> and <code>for..of</code> loops: the declared variable is treated as <em>inside</em> the loop body, and thus is handled per iteration (aka, per scope instance). No &quot;re-declaration.&quot;</p><p>OK, I know you&#39;re thinking that I sound like a broken record at this point. But let&#39;s explore how <code>const</code> impacts these looping constructs. Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> keepGoing <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
<span class="token keyword">while</span> <span class="token punctuation">(</span>keepGoing<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// ooo, a shiny constant!</span>
    <span class="token keyword">const</span> value <span class="token operator">=</span> Math<span class="token punctuation">.</span><span class="token function">random</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>value <span class="token operator">&gt;</span> <span class="token number">0.5</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        keepGoing <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Just like the <code>let</code> variant of this program we saw earlier, <code>const</code> is being run exactly once within each loop iteration, so it&#39;s safe from &quot;re-declaration&quot; troubles. But things get more complicated when we talk about <code>for</code>-loops.</p><p><code>for..in</code> and <code>for..of</code> are fine to use with <code>const</code>:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">const</span> index <span class="token keyword">in</span> students<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// this is fine</span>
<span class="token punctuation">}</span>

<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">const</span> student <span class="token keyword">of</span> students<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// this is also fine</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>But not the general <code>for</code>-loop:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">const</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> <span class="token number">3</span><span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// oops, this is going to fail with</span>
    <span class="token comment">// a Type Error after the first iteration</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>What&#39;s wrong here? We could use <code>let</code> just fine in this construct, and we asserted that it creates a new <code>i</code> for each loop iteration scope, so it doesn&#39;t even seem to be a &quot;re-declaration.&quot;</p><p>Let&#39;s mentally &quot;expand&quot; that loop like we did earlier:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token punctuation">{</span>
    <span class="token comment">// a fictional variable for illustration</span>
    <span class="token keyword">const</span> $$i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>

    <span class="token keyword">for</span> <span class="token punctuation">(</span> <span class="token punctuation">;</span> $$i <span class="token operator">&lt;</span> <span class="token number">3</span><span class="token punctuation">;</span> $$i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token comment">// here&#39;s our actual loop \`i\`!</span>
        <span class="token keyword">const</span> i <span class="token operator">=</span> $$i<span class="token punctuation">;</span>
        <span class="token comment">// ..</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Do you spot the problem? Our <code>i</code> is indeed just created once inside the loop. That&#39;s not the problem. The problem is the conceptual <code>$$i</code> that must be incremented each time with the <code>$$i++</code> expression. That&#39;s <strong>re-assignment</strong> (not &quot;re-declaration&quot;), which isn&#39;t allowed for constants.</p><p>Remember, this &quot;expanded&quot; form is only a conceptual model to help you intuit the source of the problem. You might wonder if JS could have effectively made the <code>const $$i = 0</code> instead into <code>let $ii = 0</code>, which would then allow <code>const</code> to work with our classic <code>for</code>-loop? It&#39;s possible, but then it could have introduced potentially surprising exceptions to <code>for</code>-loop semantics.</p><p>For example, it would have been a rather arbitrary (and likely confusing) nuanced exception to allow <code>i++</code> in the <code>for</code>-loop header to skirt strictness of the <code>const</code> assignment, but not allow other re-assignments of <code>i</code> inside the loop iteration, as is sometimes useful.</p><p>The straightforward answer is: <code>const</code> can&#39;t be used with the classic <code>for</code>-loop form because of the required re-assignment.</p><p>Interestingly, if you don&#39;t do re-assignment, then it&#39;s valid:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> keepGoing <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>

<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">const</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> keepGoing<span class="token punctuation">;</span> <span class="token comment">/* nothing here */</span> <span class="token punctuation">)</span> <span class="token punctuation">{</span>
    keepGoing <span class="token operator">=</span> <span class="token punctuation">(</span>Math<span class="token punctuation">.</span><span class="token function">random</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">&gt;</span> <span class="token number">0.5</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token comment">// ..</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>That works, but it&#39;s pointless. There&#39;s no reason to declare <code>i</code> in that position with a <code>const</code>, since the whole point of such a variable in that position is <strong>to be used for counting iterations</strong>. Just use a different loop form, like a <code>while</code> loop, or use a <code>let</code>!</p><h2 id="uninitialized-variables-aka-tdz" tabindex="-1"><a class="header-anchor" href="#uninitialized-variables-aka-tdz" aria-hidden="true">#</a> Uninitialized Variables (aka, TDZ)</h2><p>With <code>var</code> declarations, the variable is &quot;hoisted&quot; to the top of its scope. But it&#39;s also automatically initialized to the <code>undefined</code> value, so that the variable can be used throughout the entire scope.</p><p>However, <code>let</code> and <code>const</code> declarations are not quite the same in this respect.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code>console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// ReferenceError</span>

<span class="token keyword">let</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The result of this program is that a <code>ReferenceError</code> is thrown on the first line. Depending on your JS environment, the error message may say something like: &quot;Cannot access studentName before initialization.&quot;</p><table><thead><tr><th style="text-align:left;">NOTE:</th></tr></thead><tbody><tr><td style="text-align:left;">The error message as seen here used to be much more vague or misleading. Thankfully, several of us in the community were successfully able to lobby for JS engines to improve this error message so it more accurately tells you what&#39;s wrong!</td></tr></tbody></table><p>That error message is quite indicative of what&#39;s wrong: <code>studentName</code> exists on line 1, but it&#39;s not been initialized, so it cannot be used yet. Let&#39;s try this:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code>studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>   <span class="token comment">// let&#39;s try to initialize it!</span>
<span class="token comment">// ReferenceError</span>

console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">let</span> studentName<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>Oops. We still get the <code>ReferenceError</code>, but now on the first line where we&#39;re trying to assign to (aka, initialize!) this so-called &quot;uninitialized&quot; variable <code>studentName</code>. What&#39;s the deal!?</p><p>The real question is, how do we initialize an uninitialized variable? For <code>let</code>/<code>const</code>, the <strong>only way</strong> to do so is with an assignment attached to a declaration statement. An assignment by itself is insufficient! Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">let</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// Suzy</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Here, we are initializing the <code>studentName</code> (in this case, to <code>&quot;Suzy&quot;</code> instead of <code>undefined</code>) by way of the <code>let</code> declaration statement form that&#39;s coupled with an assignment.</p><p>Alternatively:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token comment">// ..</span>

<span class="token keyword">let</span> studentName<span class="token punctuation">;</span>
<span class="token comment">// or:</span>
<span class="token comment">// let studentName = undefined;</span>

<span class="token comment">// ..</span>

studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>

console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// Suzy</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><table><thead><tr><th style="text-align:left;">NOTE:</th></tr></thead><tbody><tr><td style="text-align:left;">That&#39;s interesting! Recall from earlier, we said that <code>var studentName;</code> is <em>not</em> the same as <code>var studentName = undefined;</code>, but here with <code>let</code>, they behave the same. The difference comes down to the fact that <code>var studentName</code> automatically initializes at the top of the scope, where <code>let studentName</code> does not.</td></tr></tbody></table><p>Remember that we&#39;ve asserted a few times so far that <em>Compiler</em> ends up removing any <code>var</code>/<code>let</code>/<code>const</code> declarators, replacing them with the instructions at the top of each scope to register the appropriate identifiers.</p><p>So if we analyze what&#39;s going on here, we see that an additional nuance is that <em>Compiler</em> is also adding an instruction in the middle of the program, at the point where the variable <code>studentName</code> was declared, to handle that declaration&#39;s auto-initialization. We cannot use the variable at any point prior to that initialization occuring. The same goes for <code>const</code> as it does for <code>let</code>.</p><p>The term coined by TC39 to refer to this <em>period of time</em> from the entering of a scope to where the auto-initialization of the variable occurs is: Temporal Dead Zone (TDZ).</p><p>The TDZ is the time window where a variable exists but is still uninitialized, and therefore cannot be accessed in any way. Only the execution of the instructions left by <em>Compiler</em> at the point of the original declaration can do that initialization. After that moment, the TDZ is done, and the variable is free to be used for the rest of the scope.</p><p>A <code>var</code> also has technically has a TDZ, but it&#39;s zero in length and thus unobservable to our programs! Only <code>let</code> and <code>const</code> have an observable TDZ.</p><p>By the way, &quot;temporal&quot; in TDZ does indeed refer to <em>time</em> not <em>position in code</em>. Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token function">askQuestion</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// ReferenceError</span>

<span class="token keyword">let</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">function</span> <span class="token function">askQuestion</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span> studentName <span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">, do you know?</span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Even though positionally the <code>console.log(..)</code> referencing <code>studentName</code> comes <em>after</em> the <code>let studentName</code> declaration, timing wise the <code>askQuestion()</code> function is invoked <em>before</em> the <code>let</code> statement is encountered, while <code>studentName</code> is still in its TDZ! Hence the error.</p><p>There&#39;s a common misconception that TDZ means <code>let</code> and <code>const</code> do not hoist. This is an inaccurate, or at least slightly misleading, claim. They definitely hoist.</p><p>The actual difference is that <code>let</code>/<code>const</code> declarations do not automatically initialize at the beginning of the scope, the way <code>var</code> does. The <em>debate</em> then is if the auto-initialization is <em>part of</em> hoisting, or not? I think auto-registration of a variable at the top of the scope (i.e., what I call &quot;hoisting&quot;) and auto-initialization at the top of the scope (to <code>undefined</code>) are distinct operations and shouldn&#39;t be lumped together under the single term &quot;hoisting.&quot;</p><p>We&#39;ve already seen that <code>let</code> and <code>const</code> don&#39;t auto-initialize at the top of the scope. But let&#39;s prove that <code>let</code> and <code>const</code> <em>do</em> hoist (auto-register at the top of the scope), courtesy of our friend shadowing (see &quot;Shadowing&quot; in Chapter 3):</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Kyle&quot;</span><span class="token punctuation">;</span>

<span class="token punctuation">{</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token comment">// ???</span>

    <span class="token comment">// ..</span>

    <span class="token keyword">let</span> studentName <span class="token operator">=</span> <span class="token string">&quot;Suzy&quot;</span><span class="token punctuation">;</span>

    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>studentName<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token comment">// Suzy</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>What&#39;s going to happen with the first <code>console.log(..)</code> statement? If <code>let studentName</code> didn&#39;t hoist to the top of the scope, then the first <code>console.log(..)</code> <em>should</em> print <code>&quot;Kyle&quot;</code>, right? At that moment, it would seem, only the outer <code>studentName</code> exists, so that&#39;s the variable <code>console.log(..)</code> should access and print.</p><p>But instead, the first <code>console.log(..)</code> throws a TDZ error, because in fact, the inner scope&#39;s <code>studentName</code> <strong>was</strong> hoisted (auto-registered at the top of the scope). What <strong>didn&#39;t</strong> happen (yet!) was the auto-initialization of that inner <code>studentName</code>; it&#39;s still uninitialized at that moment, hence the TDZ violation!</p><p>So to summarize, TDZ errors occur because <code>let</code>/<code>const</code> declarations <em>do</em> hoist their declarations to the top of their scopes, but unlike <code>var</code>, they defer the auto-initialization of their variables until the moment in the code&#39;s sequencing where the original declaration appeared. This window of time (hint: temporal), whatever its length, is the TDZ.</p><p>How can you avoid TDZ errors?</p><p>My advice: always put your <code>let</code> and <code>const</code> declarations at the top of any scope. Shrink the TDZ window to zero (or near zero) length, and then it&#39;ll be moot.</p><p>But why is TDZ even a thing? Why didn&#39;t TC39 dictate that <code>let</code>/<code>const</code> auto-initialize the way <code>var</code> does? Just be patient, we&#39;ll come back to explore the <em>why</em> of TDZ in Appendix A.</p><h2 id="finally-initialized" tabindex="-1"><a class="header-anchor" href="#finally-initialized" aria-hidden="true">#</a> Finally Initialized</h2><p>Working with variables has much more nuance than it seems at first glance. <em>Hoisting</em>, <em>(re)declaration</em>, and the <em>TDZ</em> are common sources of confusion for developers, especially those who have worked in other languages before coming to JS. Before moving on, make sure your mental model is fully grounded on these aspects of JS scope and variables.</p><p>Hoisting is generally cited as an explicit mechanism of the JS engine, but it&#39;s really more a metaphor to describe the various ways JS handles variable declarations during compilation. But even as a metaphor, hoisting offers useful structure for thinking about the life-cycle of a variable\u2014when it&#39;s created, when it&#39;s available to use, when it goes away.</p><p>Declaration and re-declaration of variables tend to cause confusion when thought of as runtime operations. But if you shift to compile-time thinking for these operations, the quirks and <em>shadows</em> diminish.</p><p>The TDZ (temporal dead zone) error is strange and frustrating when encountered. Fortunately, TDZ is relatively straightforward to avoid if you&#39;re always careful to place <code>let</code>/<code>const</code> declarations at the top of any scope.</p><p>As you successfully navigate these twists and turns of variable scope, the next chapter will lay out the factors that guide our decisions to place our declarations in various scopes, especially nested blocks.</p>`,164);function t(o,p){return a}var c=n(s,[["render",t],["__file","ch5.html.vue"]]);export{c as default};
