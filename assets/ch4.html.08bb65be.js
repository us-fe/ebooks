import{_ as a,o as e,c as o,a as t,F as p,d as n}from"./app.a957722f.js";const c={},r=n(`<h1 id="you-don-t-know-js-types-grammar" tabindex="-1"><a class="header-anchor" href="#you-don-t-know-js-types-grammar" aria-hidden="true">#</a> You Don&#39;t Know JS: Types &amp; Grammar</h1><h1 id="chapter-4-coercion" tabindex="-1"><a class="header-anchor" href="#chapter-4-coercion" aria-hidden="true">#</a> Chapter 4: Coercion</h1><p>Now that we much more fully understand JavaScript&#39;s types and values, we turn our attention to a very controversial topic: coercion.</p><p>As we mentioned in Chapter 1, the debates over whether coercion is a useful feature or a flaw in the design of the language (or somewhere in between!) have raged since day one. If you&#39;ve read other popular books on JS, you know that the overwhelmingly prevalent <em>message</em> out there is that coercion is magical, evil, confusing, and just downright a bad idea.</p><p>In the same overall spirit of this book series, rather than running away from coercion because everyone else does, or because you get bitten by some quirk, I think you should run toward that which you don&#39;t understand and seek to <em>get it</em> more fully.</p><p>Our goal is to fully explore the pros and cons (yes, there <em>are</em> pros!) of coercion, so that you can make an informed decision on its appropriateness in your program.</p><h2 id="converting-values" tabindex="-1"><a class="header-anchor" href="#converting-values" aria-hidden="true">#</a> Converting Values</h2><p>Converting a value from one type to another is often called &quot;type casting,&quot; when done explicitly, and &quot;coercion&quot; when done implicitly (forced by the rules of how a value is used).</p><p><strong>Note:</strong> It may not be obvious, but JavaScript coercions always result in one of the scalar primitive (see Chapter 2) values, like <code>string</code>, <code>number</code>, or <code>boolean</code>. There is no coercion that results in a complex value like <code>object</code> or <code>function</code>. Chapter 3 covers &quot;boxing,&quot; which wraps scalar primitive values in their <code>object</code> counterparts, but this is not really coercion in an accurate sense.</p><p>Another way these terms are often distinguished is as follows: &quot;type casting&quot; (or &quot;type conversion&quot;) occur in statically typed languages at compile time, while &quot;type coercion&quot; is a runtime conversion for dynamically typed languages.</p><p>However, in JavaScript, most people refer to all these types of conversions as <em>coercion</em>, so the way I prefer to distinguish is to say &quot;implicit coercion&quot; vs. &quot;explicit coercion.&quot;</p><p>The difference should be obvious: &quot;explicit coercion&quot; is when it is obvious from looking at the code that a type conversion is intentionally occurring, whereas &quot;implicit coercion&quot; is when the type conversion will occur as a less obvious side effect of some other intentional operation.</p><p>For example, consider these two approaches to coercion:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> b <span class="token operator">=</span> a <span class="token operator">+</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>			<span class="token comment">// implicit coercion</span>

<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token function">String</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// explicit coercion</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>For <code>b</code>, the coercion that occurs happens implicitly, because the <code>+</code> operator combined with one of the operands being a <code>string</code> value (<code>&quot;&quot;</code>) will insist on the operation being a <code>string</code> concatenation (adding two strings together), which <em>as a (hidden) side effect</em> will force the <code>42</code> value in <code>a</code> to be coerced to its <code>string</code> equivalent: <code>&quot;42&quot;</code>.</p><p>By contrast, the <code>String(..)</code> function makes it pretty obvious that it&#39;s explicitly taking the value in <code>a</code> and coercing it to a <code>string</code> representation.</p><p>Both approaches accomplish the same effect: <code>&quot;42&quot;</code> comes from <code>42</code>. But it&#39;s the <em>how</em> that is at the heart of the heated debates over JavaScript coercion.</p><p><strong>Note:</strong> Technically, there&#39;s some nuanced behavioral difference here beyond the stylistic difference. We cover that in more detail later in the chapter, in the &quot;Implicitly: Strings &lt;--&gt; Numbers&quot; section.</p><p>The terms &quot;explicit&quot; and &quot;implicit,&quot; or &quot;obvious&quot; and &quot;hidden side effect,&quot; are <em>relative</em>.</p><p>If you know exactly what <code>a + &quot;&quot;</code> is doing and you&#39;re intentionally doing that to coerce to a <code>string</code>, you might feel the operation is sufficiently &quot;explicit.&quot; Conversely, if you&#39;ve never seen the <code>String(..)</code> function used for <code>string</code> coercion, its behavior might seem hidden enough as to feel &quot;implicit&quot; to you.</p><p>But we&#39;re having this discussion of &quot;explicit&quot; vs. &quot;implicit&quot; based on the likely opinions of an <em>average, reasonably informed, but not expert or JS specification devotee</em> developer. To whatever extent you do or do not find yourself fitting neatly in that bucket, you will need to adjust your perspective on our observations here accordingly.</p><p>Just remember: it&#39;s often rare that we write our code and are the only ones who ever read it. Even if you&#39;re an expert on all the ins and outs of JS, consider how a less experienced teammate of yours will feel when they read your code. Will it be &quot;explicit&quot; or &quot;implicit&quot; to them in the same way it is for you?</p><h2 id="abstract-value-operations" tabindex="-1"><a class="header-anchor" href="#abstract-value-operations" aria-hidden="true">#</a> Abstract Value Operations</h2><p>Before we can explore <em>explicit</em> vs <em>implicit</em> coercion, we need to learn the basic rules that govern how values <em>become</em> either a <code>string</code>, <code>number</code>, or <code>boolean</code>. The ES5 spec in section 9 defines several &quot;abstract operations&quot; (fancy spec-speak for &quot;internal-only operation&quot;) with the rules of value conversion. We will specifically pay attention to: <code>ToString</code>, <code>ToNumber</code>, and <code>ToBoolean</code>, and to a lesser extent, <code>ToPrimitive</code>.</p><h3 id="tostring" tabindex="-1"><a class="header-anchor" href="#tostring" aria-hidden="true">#</a> <code>ToString</code></h3><p>When any non-<code>string</code> value is coerced to a <code>string</code> representation, the conversion is handled by the <code>ToString</code> abstract operation in section 9.8 of the specification.</p><p>Built-in primitive values have natural stringification: <code>null</code> becomes <code>&quot;null&quot;</code>, <code>undefined</code> becomes <code>&quot;undefined&quot;</code> and <code>true</code> becomes <code>&quot;true&quot;</code>. <code>number</code>s are generally expressed in the natural way you&#39;d expect, but as we discussed in Chapter 2, very small or very large <code>numbers</code> are represented in exponent form:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token comment">// multiplying \`1.07\` by \`1000\`, seven times over</span>
<span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">1.07</span> <span class="token operator">*</span> <span class="token number">1000</span> <span class="token operator">*</span> <span class="token number">1000</span> <span class="token operator">*</span> <span class="token number">1000</span> <span class="token operator">*</span> <span class="token number">1000</span> <span class="token operator">*</span> <span class="token number">1000</span> <span class="token operator">*</span> <span class="token number">1000</span> <span class="token operator">*</span> <span class="token number">1000</span><span class="token punctuation">;</span>

<span class="token comment">// seven times three digits =&gt; 21 digits</span>
a<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;1.07e21&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>For regular objects, unless you specify your own, the default <code>toString()</code> (located in <code>Object.prototype.toString()</code>) will return the <em>internal <code>[[Class]]</code></em> (see Chapter 3), like for instance <code>&quot;[object Object]&quot;</code>.</p><p>But as shown earlier, if an object has its own <code>toString()</code> method on it, and you use that object in a <code>string</code>-like way, its <code>toString()</code> will automatically be called, and the <code>string</code> result of that call will be used instead.</p><p><strong>Note:</strong> The way an object is coerced to a <code>string</code> technically goes through the <code>ToPrimitive</code> abstract operation (ES5 spec, section 9.1), but those nuanced details are covered in more detail in the <code>ToNumber</code> section later in this chapter, so we will skip over them here.</p><p>Arrays have an overridden default <code>toString()</code> that stringifies as the (string) concatenation of all its values (each stringified themselves), with <code>&quot;,&quot;</code> in between each value:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">,</span><span class="token number">3</span><span class="token punctuation">]</span><span class="token punctuation">;</span>

a<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;1,2,3&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Again, <code>toString()</code> can either be called explicitly, or it will automatically be called if a non-<code>string</code> is used in a <code>string</code> context.</p><h4 id="json-stringification" tabindex="-1"><a class="header-anchor" href="#json-stringification" aria-hidden="true">#</a> JSON Stringification</h4><p>Another task that seems awfully related to <code>ToString</code> is when you use the <code>JSON.stringify(..)</code> utility to serialize a value to a JSON-compatible <code>string</code> value.</p><p>It&#39;s important to note that this stringification is not exactly the same thing as coercion. But since it&#39;s related to the <code>ToString</code> rules above, we&#39;ll take a slight diversion to cover JSON stringification behaviors here.</p><p>For most simple values, JSON stringification behaves basically the same as <code>toString()</code> conversions, except that the serialization result is <em>always a <code>string</code></em>:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> <span class="token number">42</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// &quot;42&quot;</span>
<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> <span class="token string">&quot;42&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// &quot;&quot;42&quot;&quot; (a string with a quoted string value in it)</span>
<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> <span class="token keyword">null</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// &quot;null&quot;</span>
<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> <span class="token boolean">true</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// &quot;true&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Any <em>JSON-safe</em> value can be stringified by <code>JSON.stringify(..)</code>. But what is <em>JSON-safe</em>? Any value that can be represented validly in a JSON representation.</p><p>It may be easier to consider values that are <strong>not</strong> JSON-safe. Some examples: <code>undefined</code>s, <code>function</code>s, (ES6+) <code>symbol</code>s, and <code>object</code>s with circular references (where property references in an object structure create a never-ending cycle through each other). These are all illegal values for a standard JSON structure, mostly because they aren&#39;t portable to other languages that consume JSON values.</p><p>The <code>JSON.stringify(..)</code> utility will automatically omit <code>undefined</code>, <code>function</code>, and <code>symbol</code> values when it comes across them. If such a value is found in an <code>array</code>, that value is replaced by <code>null</code> (so that the array position information isn&#39;t altered). If found as a property of an <code>object</code>, that property will simply be excluded.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> <span class="token keyword">undefined</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>					<span class="token comment">// undefined</span>
<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span><span class="token punctuation">}</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>					<span class="token comment">// undefined</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token keyword">undefined</span><span class="token punctuation">,</span><span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">,</span><span class="token number">4</span><span class="token punctuation">]</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// &quot;[1,null,null,4]&quot;</span>
<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> <span class="token punctuation">{</span> <span class="token literal-property property">a</span><span class="token operator">:</span><span class="token number">2</span><span class="token punctuation">,</span> <span class="token function-variable function">b</span><span class="token operator">:</span><span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span><span class="token punctuation">}</span> <span class="token punctuation">}</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// &quot;{&quot;a&quot;:2}&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>But if you try to <code>JSON.stringify(..)</code> an <code>object</code> with circular reference(s) in it, an error will be thrown.</p><p>JSON stringification has the special behavior that if an <code>object</code> value has a <code>toJSON()</code> method defined, this method will be called first to get a value to use for serialization.</p><p>If you intend to JSON stringify an object that may contain illegal JSON value(s), or if you just have values in the <code>object</code> that aren&#39;t appropriate for the serialization, you should define a <code>toJSON()</code> method for it that returns a <em>JSON-safe</em> version of the <code>object</code>.</p><p>For example:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> o <span class="token operator">=</span> <span class="token punctuation">{</span> <span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">{</span>
	<span class="token literal-property property">b</span><span class="token operator">:</span> <span class="token number">42</span><span class="token punctuation">,</span>
	<span class="token literal-property property">c</span><span class="token operator">:</span> o<span class="token punctuation">,</span>
	<span class="token function-variable function">d</span><span class="token operator">:</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span><span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token comment">// create a circular reference inside \`a\`</span>
o<span class="token punctuation">.</span>e <span class="token operator">=</span> a<span class="token punctuation">;</span>

<span class="token comment">// would throw an error on the circular reference</span>
<span class="token comment">// JSON.stringify( a );</span>

<span class="token comment">// define a custom JSON value serialization</span>
a<span class="token punctuation">.</span><span class="token function-variable function">toJSON</span> <span class="token operator">=</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token comment">// only include the \`b\` property for serialization</span>
	<span class="token keyword">return</span> <span class="token punctuation">{</span> <span class="token literal-property property">b</span><span class="token operator">:</span> <span class="token keyword">this</span><span class="token punctuation">.</span>b <span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;{&quot;b&quot;:42}&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br></div></div><p>It&#39;s a very common misconception that <code>toJSON()</code> should return a JSON stringification representation. That&#39;s probably incorrect, unless you&#39;re wanting to actually stringify the <code>string</code> itself (usually not!). <code>toJSON()</code> should return the actual regular value (of whatever type) that&#39;s appropriate, and <code>JSON.stringify(..)</code> itself will handle the stringification.</p><p>In other words, <code>toJSON()</code> should be interpreted as &quot;to a JSON-safe value suitable for stringification,&quot; not &quot;to a JSON string&quot; as many developers mistakenly assume.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">{</span>
	<span class="token literal-property property">val</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">,</span><span class="token number">3</span><span class="token punctuation">]</span><span class="token punctuation">,</span>

	<span class="token comment">// probably correct!</span>
	<span class="token function-variable function">toJSON</span><span class="token operator">:</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
		<span class="token keyword">return</span> <span class="token keyword">this</span><span class="token punctuation">.</span>val<span class="token punctuation">.</span><span class="token function">slice</span><span class="token punctuation">(</span> <span class="token number">1</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
	<span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">{</span>
	<span class="token literal-property property">val</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">,</span><span class="token number">3</span><span class="token punctuation">]</span><span class="token punctuation">,</span>

	<span class="token comment">// probably incorrect!</span>
	<span class="token function-variable function">toJSON</span><span class="token operator">:</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
		<span class="token keyword">return</span> <span class="token string">&quot;[&quot;</span> <span class="token operator">+</span>
			<span class="token keyword">this</span><span class="token punctuation">.</span>val<span class="token punctuation">.</span><span class="token function">slice</span><span class="token punctuation">(</span> <span class="token number">1</span> <span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">join</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span>
		<span class="token string">&quot;]&quot;</span><span class="token punctuation">;</span>
	<span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;[2,3]&quot;</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;&quot;[2,3]&quot;&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br></div></div><p>In the second call, we stringified the returned <code>string</code> rather than the <code>array</code> itself, which was probably not what we wanted to do.</p><p>While we&#39;re talking about <code>JSON.stringify(..)</code>, let&#39;s discuss some lesser-known functionalities that can still be very useful.</p><p>An optional second argument can be passed to <code>JSON.stringify(..)</code> that is called <em>replacer</em>. This argument can either be an <code>array</code> or a <code>function</code>. It&#39;s used to customize the recursive serialization of an <code>object</code> by providing a filtering mechanism for which properties should and should not be included, in a similar way to how <code>toJSON()</code> can prepare a value for serialization.</p><p>If <em>replacer</em> is an <code>array</code>, it should be an <code>array</code> of <code>string</code>s, each of which will specify a property name that is allowed to be included in the serialization of the <code>object</code>. If a property exists that isn&#39;t in this list, it will be skipped.</p><p>If <em>replacer</em> is a <code>function</code>, it will be called once for the <code>object</code> itself, and then once for each property in the <code>object</code>, and each time is passed two arguments, <em>key</em> and <em>value</em>. To skip a <em>key</em> in the serialization, return <code>undefined</code>. Otherwise, return the <em>value</em> provided.</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">{</span>
	<span class="token literal-property property">b</span><span class="token operator">:</span> <span class="token number">42</span><span class="token punctuation">,</span>
	<span class="token literal-property property">c</span><span class="token operator">:</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">,</span>
	<span class="token literal-property property">d</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">,</span><span class="token number">3</span><span class="token punctuation">]</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> a<span class="token punctuation">,</span> <span class="token punctuation">[</span><span class="token string">&quot;b&quot;</span><span class="token punctuation">,</span><span class="token string">&quot;c&quot;</span><span class="token punctuation">]</span> <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;{&quot;b&quot;:42,&quot;c&quot;:&quot;42&quot;}&quot;</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> a<span class="token punctuation">,</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token parameter">k<span class="token punctuation">,</span>v</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
	<span class="token keyword">if</span> <span class="token punctuation">(</span>k <span class="token operator">!==</span> <span class="token string">&quot;c&quot;</span><span class="token punctuation">)</span> <span class="token keyword">return</span> v<span class="token punctuation">;</span>
<span class="token punctuation">}</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// &quot;{&quot;b&quot;:42,&quot;d&quot;:[1,2,3]}&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p><strong>Note:</strong> In the <code>function</code> <em>replacer</em> case, the key argument <code>k</code> is <code>undefined</code> for the first call (where the <code>a</code> object itself is being passed in). The <code>if</code> statement <strong>filters out</strong> the property named <code>&quot;c&quot;</code>. Stringification is recursive, so the <code>[1,2,3]</code> array has each of its values (<code>1</code>, <code>2</code>, and <code>3</code>) passed as <code>v</code> to <em>replacer</em>, with indexes (<code>0</code>, <code>1</code>, and <code>2</code>) as <code>k</code>.</p><p>A third optional argument can also be passed to <code>JSON.stringify(..)</code>, called <em>space</em>, which is used as indentation for prettier human-friendly output. <em>space</em> can be a positive integer to indicate how many space characters should be used at each indentation level. Or, <em>space</em> can be a <code>string</code>, in which case up to the first ten characters of its value will be used for each indentation level.</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">{</span>
	<span class="token literal-property property">b</span><span class="token operator">:</span> <span class="token number">42</span><span class="token punctuation">,</span>
	<span class="token literal-property property">c</span><span class="token operator">:</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">,</span>
	<span class="token literal-property property">d</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">,</span><span class="token number">3</span><span class="token punctuation">]</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> a<span class="token punctuation">,</span> <span class="token keyword">null</span><span class="token punctuation">,</span> <span class="token number">3</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// &quot;{</span>
<span class="token comment">//    &quot;b&quot;: 42,</span>
<span class="token comment">//    &quot;c&quot;: &quot;42&quot;,</span>
<span class="token comment">//    &quot;d&quot;: [</span>
<span class="token comment">//       1,</span>
<span class="token comment">//       2,</span>
<span class="token comment">//       3</span>
<span class="token comment">//    ]</span>
<span class="token comment">// }&quot;</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> a<span class="token punctuation">,</span> <span class="token keyword">null</span><span class="token punctuation">,</span> <span class="token string">&quot;-----&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// &quot;{</span>
<span class="token comment">// -----&quot;b&quot;: 42,</span>
<span class="token comment">// -----&quot;c&quot;: &quot;42&quot;,</span>
<span class="token comment">// -----&quot;d&quot;: [</span>
<span class="token comment">// ----------1,</span>
<span class="token comment">// ----------2,</span>
<span class="token comment">// ----------3</span>
<span class="token comment">// -----]</span>
<span class="token comment">// }&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br><span class="line-number">27</span><br></div></div><p>Remember, <code>JSON.stringify(..)</code> is not directly a form of coercion. We covered it here, however, for two reasons that relate its behavior to <code>ToString</code> coercion:</p><ol><li><code>string</code>, <code>number</code>, <code>boolean</code>, and <code>null</code> values all stringify for JSON basically the same as how they coerce to <code>string</code> values via the rules of the <code>ToString</code> abstract operation.</li><li>If you pass an <code>object</code> value to <code>JSON.stringify(..)</code>, and that <code>object</code> has a <code>toJSON()</code> method on it, <code>toJSON()</code> is automatically called to (sort of) &quot;coerce&quot; the value to be <em>JSON-safe</em> before stringification.</li></ol><h3 id="tonumber" tabindex="-1"><a class="header-anchor" href="#tonumber" aria-hidden="true">#</a> <code>ToNumber</code></h3><p>If any non-<code>number</code> value is used in a way that requires it to be a <code>number</code>, such as a mathematical operation, the ES5 spec defines the <code>ToNumber</code> abstract operation in section 9.3.</p><p>For example, <code>true</code> becomes <code>1</code> and <code>false</code> becomes <code>0</code>. <code>undefined</code> becomes <code>NaN</code>, but (curiously) <code>null</code> becomes <code>0</code>.</p><p><code>ToNumber</code> for a <code>string</code> value essentially works for the most part like the rules/syntax for numeric literals (see Chapter 3). If it fails, the result is <code>NaN</code> (instead of a syntax error as with <code>number</code> literals). One example difference is that <code>0</code>-prefixed octal numbers are not handled as octals (just as normal base-10 decimals) in this operation, though such octals are valid as <code>number</code> literals (see Chapter 2).</p><p><strong>Note:</strong> The differences between <code>number</code> literal grammar and <code>ToNumber</code> on a <code>string</code> value are subtle and highly nuanced, and thus will not be covered further here. Consult section 9.3.1 of the ES5 spec for more information.</p><p>Objects (and arrays) will first be converted to their primitive value equivalent, and the resulting value (if a primitive but not already a <code>number</code>) is coerced to a <code>number</code> according to the <code>ToNumber</code> rules just mentioned.</p><p>To convert to this primitive value equivalent, the <code>ToPrimitive</code> abstract operation (ES5 spec, section 9.1) will consult the value (using the internal <code>DefaultValue</code> operation -- ES5 spec, section 8.12.8) in question to see if it has a <code>valueOf()</code> method. If <code>valueOf()</code> is available and it returns a primitive value, <em>that</em> value is used for the coercion. If not, but <code>toString()</code> is available, it will provide the value for the coercion.</p><p>If neither operation can provide a primitive value, a <code>TypeError</code> is thrown.</p><p>As of ES5, you can create such a noncoercible object -- one without <code>valueOf()</code> and <code>toString()</code> -- if it has a <code>null</code> value for its <code>[[Prototype]]</code>, typically created with <code>Object.create(null)</code>. See the <em>this &amp; Object Prototypes</em> title of this series for more information on <code>[[Prototype]]</code>s.</p><p><strong>Note:</strong> We cover how to coerce to <code>number</code>s later in this chapter in detail, but for this next code snippet, just assume the <code>Number(..)</code> function does so.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">{</span>
	<span class="token function-variable function">valueOf</span><span class="token operator">:</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
		<span class="token keyword">return</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>
	<span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">{</span>
	<span class="token function-variable function">toString</span><span class="token operator">:</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
		<span class="token keyword">return</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>
	<span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">4</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
c<span class="token punctuation">.</span><span class="token function-variable function">toString</span> <span class="token operator">=</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
	<span class="token keyword">return</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">join</span><span class="token punctuation">(</span> <span class="token string">&quot;&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// &quot;42&quot;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token function">Number</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>			<span class="token comment">// 42</span>
<span class="token function">Number</span><span class="token punctuation">(</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span>			<span class="token comment">// 42</span>
<span class="token function">Number</span><span class="token punctuation">(</span> c <span class="token punctuation">)</span><span class="token punctuation">;</span>			<span class="token comment">// 42</span>
<span class="token function">Number</span><span class="token punctuation">(</span> <span class="token string">&quot;&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>			<span class="token comment">// 0</span>
<span class="token function">Number</span><span class="token punctuation">(</span> <span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>			<span class="token comment">// 0</span>
<span class="token function">Number</span><span class="token punctuation">(</span> <span class="token punctuation">[</span> <span class="token string">&quot;abc&quot;</span> <span class="token punctuation">]</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// NaN</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br></div></div><h3 id="toboolean" tabindex="-1"><a class="header-anchor" href="#toboolean" aria-hidden="true">#</a> <code>ToBoolean</code></h3><p>Next, let&#39;s have a little chat about how <code>boolean</code>s behave in JS. There&#39;s <strong>lots of confusion and misconception</strong> floating out there around this topic, so pay close attention!</p><p>First and foremost, JS has actual keywords <code>true</code> and <code>false</code>, and they behave exactly as you&#39;d expect of <code>boolean</code> values. It&#39;s a common misconception that the values <code>1</code> and <code>0</code> are identical to <code>true</code>/<code>false</code>. While that may be true in other languages, in JS the <code>number</code>s are <code>number</code>s and the <code>boolean</code>s are <code>boolean</code>s. You can coerce <code>1</code> to <code>true</code> (and vice versa) or <code>0</code> to <code>false</code> (and vice versa). But they&#39;re not the same.</p><h4 id="falsy-values" tabindex="-1"><a class="header-anchor" href="#falsy-values" aria-hidden="true">#</a> Falsy Values</h4><p>But that&#39;s not the end of the story. We need to discuss how values other than the two <code>boolean</code>s behave whenever you coerce <em>to</em> their <code>boolean</code> equivalent.</p><p>All of JavaScript&#39;s values can be divided into two categories:</p><ol><li>values that will become <code>false</code> if coerced to <code>boolean</code></li><li>everything else (which will obviously become <code>true</code>)</li></ol><p>I&#39;m not just being facetious. The JS spec defines a specific, narrow list of values that will coerce to <code>false</code> when coerced to a <code>boolean</code> value.</p><p>How do we know what the list of values is? In the ES5 spec, section 9.2 defines a <code>ToBoolean</code> abstract operation, which says exactly what happens for all the possible values when you try to coerce them &quot;to boolean.&quot;</p><p>From that table, we get the following as the so-called &quot;falsy&quot; values list:</p><ul><li><code>undefined</code></li><li><code>null</code></li><li><code>false</code></li><li><code>+0</code>, <code>-0</code>, and <code>NaN</code></li><li><code>&quot;&quot;</code></li></ul><p>That&#39;s it. If a value is on that list, it&#39;s a &quot;falsy&quot; value, and it will coerce to <code>false</code> if you force a <code>boolean</code> coercion on it.</p><p>By logical conclusion, if a value is <em>not</em> on that list, it must be on <em>another list</em>, which we call the &quot;truthy&quot; values list. But JS doesn&#39;t really define a &quot;truthy&quot; list per se. It gives some examples, such as saying explicitly that all objects are truthy, but mostly the spec just implies: <strong>anything not explicitly on the falsy list is therefore truthy.</strong></p><h4 id="falsy-objects" tabindex="-1"><a class="header-anchor" href="#falsy-objects" aria-hidden="true">#</a> Falsy Objects</h4><p>Wait a minute, that section title even sounds contradictory. I literally <em>just said</em> the spec calls all objects truthy, right? There should be no such thing as a &quot;falsy object.&quot;</p><p>What could that possibly even mean?</p><p>You might be tempted to think it means an object wrapper (see Chapter 3) around a falsy value (such as <code>&quot;&quot;</code>, <code>0</code> or <code>false</code>). But don&#39;t fall into that <em>trap</em>.</p><p><strong>Note:</strong> That&#39;s a subtle specification joke some of you may get.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Boolean</span><span class="token punctuation">(</span> <span class="token boolean">false</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Number</span><span class="token punctuation">(</span> <span class="token number">0</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">String</span><span class="token punctuation">(</span> <span class="token string">&quot;&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>We know all three values here are objects (see Chapter 3) wrapped around obviously falsy values. But do these objects behave as <code>true</code> or as <code>false</code>? That&#39;s easy to answer:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token function">Boolean</span><span class="token punctuation">(</span> a <span class="token operator">&amp;&amp;</span> b <span class="token operator">&amp;&amp;</span> c <span class="token punctuation">)</span><span class="token punctuation">;</span>

d<span class="token punctuation">;</span> <span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>So, all three behave as <code>true</code>, as that&#39;s the only way <code>d</code> could end up as <code>true</code>.</p><p><strong>Tip:</strong> Notice the <code>Boolean( .. )</code> wrapped around the <code>a &amp;&amp; b &amp;&amp; c</code> expression -- you might wonder why that&#39;s there. We&#39;ll come back to that later in this chapter, so make a mental note of it. For a sneak-peek (trivia-wise), try for yourself what <code>d</code> will be if you just do <code>d = a &amp;&amp; b &amp;&amp; c</code> without the <code>Boolean( .. )</code> call!</p><p>So, if &quot;falsy objects&quot; are <strong>not just objects wrapped around falsy values</strong>, what the heck are they?</p><p>The tricky part is that they can show up in your JS program, but they&#39;re not actually part of JavaScript itself.</p><p><strong>What!?</strong></p><p>There are certain cases where browsers have created their own sort of <em>exotic</em> values behavior, namely this idea of &quot;falsy objects,&quot; on top of regular JS semantics.</p><p>A &quot;falsy object&quot; is a value that looks and acts like a normal object (properties, etc.), but when you coerce it to a <code>boolean</code>, it coerces to a <code>false</code> value.</p><p><strong>Why!?</strong></p><p>The most well-known case is <code>document.all</code>: an array-like (object) provided to your JS program <em>by the DOM</em> (not the JS engine itself), which exposes elements in your page to your JS program. It <em>used</em> to behave like a normal object--it would act truthy. But not anymore.</p><p><code>document.all</code> itself was never really &quot;standard&quot; and has long since been deprecated/abandoned.</p><p>&quot;Can&#39;t they just remove it, then?&quot; Sorry, nice try. Wish they could. But there&#39;s far too many legacy JS code bases out there that rely on using it.</p><p>So, why make it act falsy? Because coercions of <code>document.all</code> to <code>boolean</code> (like in <code>if</code> statements) were almost always used as a means of detecting old, nonstandard IE.</p><p>IE has long since come up to standards compliance, and in many cases is pushing the web forward as much or more than any other browser. But all that old <code>if (document.all) { /* it&#39;s IE */ }</code> code is still out there, and much of it is probably never going away. All this legacy code is still assuming it&#39;s running in decade-old IE, which just leads to bad browsing experience for IE users.</p><p>So, we can&#39;t remove <code>document.all</code> completely, but IE doesn&#39;t want <code>if (document.all) { .. }</code> code to work anymore, so that users in modern IE get new, standards-compliant code logic.</p><p>&quot;What should we do?&quot; **&quot;I&#39;ve got it! Let&#39;s bastardize the JS type system and pretend that <code>document.all</code> is falsy!&quot;</p><p>Ugh. That sucks. It&#39;s a crazy gotcha that most JS developers don&#39;t understand. But the alternative (doing nothing about the above no-win problems) sucks <em>just a little bit more</em>.</p><p>So... that&#39;s what we&#39;ve got: crazy, nonstandard &quot;falsy objects&quot; added to JavaScript by the browsers. Yay!</p><h4 id="truthy-values" tabindex="-1"><a class="header-anchor" href="#truthy-values" aria-hidden="true">#</a> Truthy Values</h4><p>Back to the truthy list. What exactly are the truthy values? Remember: <strong>a value is truthy if it&#39;s not on the falsy list.</strong></p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;false&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token string">&quot;0&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token string">&quot;&#39;&#39;&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token function">Boolean</span><span class="token punctuation">(</span> a <span class="token operator">&amp;&amp;</span> b <span class="token operator">&amp;&amp;</span> c <span class="token punctuation">)</span><span class="token punctuation">;</span>

d<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>What value do you expect <code>d</code> to have here? It&#39;s gotta be either <code>true</code> or <code>false</code>.</p><p>It&#39;s <code>true</code>. Why? Because despite the contents of those <code>string</code> values looking like falsy values, the <code>string</code> values themselves are all truthy, because <code>&quot;&quot;</code> is the only <code>string</code> value on the falsy list.</p><p>What about these?</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>				<span class="token comment">// empty array -- truthy or falsy?</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">;</span>				<span class="token comment">// empty object -- truthy or falsy?</span>
<span class="token keyword">var</span> <span class="token function-variable function">c</span> <span class="token operator">=</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">;</span>	<span class="token comment">// empty function -- truthy or falsy?</span>

<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token function">Boolean</span><span class="token punctuation">(</span> a <span class="token operator">&amp;&amp;</span> b <span class="token operator">&amp;&amp;</span> c <span class="token punctuation">)</span><span class="token punctuation">;</span>

d<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Yep, you guessed it, <code>d</code> is still <code>true</code> here. Why? Same reason as before. Despite what it may seem like, <code>[]</code>, <code>{}</code>, and <code>function(){}</code> are <em>not</em> on the falsy list, and thus are truthy values.</p><p>In other words, the truthy list is infinitely long. It&#39;s impossible to make such a list. You can only make a finite falsy list and consult <em>it</em>.</p><p>Take five minutes, write the falsy list on a post-it note for your computer monitor, or memorize it if you prefer. Either way, you&#39;ll easily be able to construct a virtual truthy list whenever you need it by simply asking if it&#39;s on the falsy list or not.</p><p>The importance of truthy and falsy is in understanding how a value will behave if you coerce it (either explicitly or implicitly) to a <code>boolean</code> value. Now that you have those two lists in mind, we can dive into coercion examples themselves.</p><h2 id="explicit-coercion" tabindex="-1"><a class="header-anchor" href="#explicit-coercion" aria-hidden="true">#</a> Explicit Coercion</h2><p><em>Explicit</em> coercion refers to type conversions that are obvious and explicit. There&#39;s a wide range of type conversion usage that clearly falls under the <em>explicit</em> coercion category for most developers.</p><p>The goal here is to identify patterns in our code where we can make it clear and obvious that we&#39;re converting a value from one type to another, so as to not leave potholes for future developers to trip into. The more explicit we are, the more likely someone later will be able to read our code and understand without undue effort what our intent was.</p><p>It would be hard to find any salient disagreements with <em>explicit</em> coercion, as it most closely aligns with how the commonly accepted practice of type conversion works in statically typed languages. As such, we&#39;ll take for granted (for now) that <em>explicit</em> coercion can be agreed upon to not be evil or controversial. We&#39;ll revisit this later, though.</p><h3 id="explicitly-strings-numbers" tabindex="-1"><a class="header-anchor" href="#explicitly-strings-numbers" aria-hidden="true">#</a> Explicitly: Strings &lt;--&gt; Numbers</h3><p>We&#39;ll start with the simplest and perhaps most common coercion operation: coercing values between <code>string</code> and <code>number</code> representation.</p><p>To coerce between <code>string</code>s and <code>number</code>s, we use the built-in <code>String(..)</code> and <code>Number(..)</code> functions (which we referred to as &quot;native constructors&quot; in Chapter 3), but <strong>very importantly</strong>, we do not use the <code>new</code> keyword in front of them. As such, we&#39;re not creating object wrappers.</p><p>Instead, we&#39;re actually <em>explicitly coercing</em> between the two types:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token function">String</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token string">&quot;3.14&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token function">Number</span><span class="token punctuation">(</span> c <span class="token punctuation">)</span><span class="token punctuation">;</span>

b<span class="token punctuation">;</span> <span class="token comment">// &quot;42&quot;</span>
d<span class="token punctuation">;</span> <span class="token comment">// 3.14</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><code>String(..)</code> coerces from any other value to a primitive <code>string</code> value, using the rules of the <code>ToString</code> operation discussed earlier. <code>Number(..)</code> coerces from any other value to a primitive <code>number</code> value, using the rules of the <code>ToNumber</code> operation discussed earlier.</p><p>I call this <em>explicit</em> coercion because in general, it&#39;s pretty obvious to most developers that the end result of these operations is the applicable type conversion.</p><p>In fact, this usage actually looks a lot like it does in some other statically typed languages.</p><p>For example, in C/C++, you can say either <code>(int)x</code> or <code>int(x)</code>, and both will convert the value in <code>x</code> to an integer. Both forms are valid, but many prefer the latter, which kinda looks like a function call. In JavaScript, when you say <code>Number(x)</code>, it looks awfully similar. Does it matter that it&#39;s <em>actually</em> a function call in JS? Not really.</p><p>Besides <code>String(..)</code> and <code>Number(..)</code>, there are other ways to &quot;explicitly&quot; convert these values between <code>string</code> and <code>number</code>:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> a<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token string">&quot;3.14&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token operator">+</span>c<span class="token punctuation">;</span>

b<span class="token punctuation">;</span> <span class="token comment">// &quot;42&quot;</span>
d<span class="token punctuation">;</span> <span class="token comment">// 3.14</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Calling <code>a.toString()</code> is ostensibly explicit (pretty clear that &quot;toString&quot; means &quot;to a string&quot;), but there&#39;s some hidden implicitness here. <code>toString()</code> cannot be called on a <em>primitive</em> value like <code>42</code>. So JS automatically &quot;boxes&quot; (see Chapter 3) <code>42</code> in an object wrapper, so that <code>toString()</code> can be called against the object. In other words, you might call it &quot;explicitly implicit.&quot;</p><p><code>+c</code> here is showing the <em>unary operator</em> form (operator with only one operand) of the <code>+</code> operator. Instead of performing mathematic addition (or string concatenation -- see below), the unary <code>+</code> explicitly coerces its operand (<code>c</code>) to a <code>number</code> value.</p><p>Is <code>+c</code> <em>explicit</em> coercion? Depends on your experience and perspective. If you know (which you do, now!) that unary <code>+</code> is explicitly intended for <code>number</code> coercion, then it&#39;s pretty explicit and obvious. However, if you&#39;ve never seen it before, it can seem awfully confusing, implicit, with hidden side effects, etc.</p><p><strong>Note:</strong> The generally accepted perspective in the open-source JS community is that unary <code>+</code> is an accepted form of <em>explicit</em> coercion.</p><p>Even if you really like the <code>+c</code> form, there are definitely places where it can look awfully confusing. Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token string">&quot;3.14&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token number">5</span><span class="token operator">+</span> <span class="token operator">+</span>c<span class="token punctuation">;</span>

d<span class="token punctuation">;</span> <span class="token comment">// 8.14</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The unary <code>-</code> operator also coerces like <code>+</code> does, but it also flips the sign of the number. However, you cannot put two <code>--</code> next to each other to unflip the sign, as that&#39;s parsed as the decrement operator. Instead, you would need to do: <code>- -&quot;3.14&quot;</code> with a space in between, and that would result in coercion to <code>3.14</code>.</p><p>You can probably dream up all sorts of hideous combinations of binary operators (like <code>+</code> for addition) next to the unary form of an operator. Here&#39;s another crazy example:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token number">1</span> <span class="token operator">+</span> <span class="token operator">-</span> <span class="token operator">+</span> <span class="token operator">+</span> <span class="token operator">+</span> <span class="token operator">-</span> <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">;</span>	<span class="token comment">// 2</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>You should strongly consider avoiding unary <code>+</code> (or <code>-</code>) coercion when it&#39;s immediately adjacent to other operators. While the above works, it would almost universally be considered a bad idea. Even <code>d = +c</code> (or <code>d =+ c</code> for that matter!) can far too easily be confused for <code>d += c</code>, which is entirely different!</p><p><strong>Note:</strong> Another extremely confusing place for unary <code>+</code> to be used adjacent to another operator would be the <code>++</code> increment operator and <code>--</code> decrement operator. For example: <code>a +++b</code>, <code>a + ++b</code>, and <code>a + + +b</code>. See &quot;Expression Side-Effects&quot; in Chapter 5 for more about <code>++</code>.</p><p>Remember, we&#39;re trying to be explicit and <strong>reduce</strong> confusion, not make it much worse!</p><h4 id="date-to-number" tabindex="-1"><a class="header-anchor" href="#date-to-number" aria-hidden="true">#</a> <code>Date</code> To <code>number</code></h4><p>Another common usage of the unary <code>+</code> operator is to coerce a <code>Date</code> object into a <code>number</code>, because the result is the unix timestamp (milliseconds elapsed since 1 January 1970 00:00:00 UTC) representation of the date/time value:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Date</span><span class="token punctuation">(</span> <span class="token string">&quot;Mon, 18 Aug 2014 08:53:06 CDT&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token operator">+</span>d<span class="token punctuation">;</span> <span class="token comment">// 1408369986000</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>The most common usage of this idiom is to get the current <em>now</em> moment as a timestamp, such as:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> timestamp <span class="token operator">=</span> <span class="token operator">+</span><span class="token keyword">new</span> <span class="token class-name">Date</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>Note:</strong> Some developers are aware of a peculiar syntactic &quot;trick&quot; in JavaScript, which is that the <code>()</code> set on a constructor call (a function called with <code>new</code>) is <em>optional</em> if there are no arguments to pass. So you may run across the <code>var timestamp = +new Date;</code> form. However, not all developers agree that omitting the <code>()</code> improves readability, as it&#39;s an uncommon syntax exception that only applies to the <code>new fn()</code> call form and not the regular <code>fn()</code> call form.</p><p>But coercion is not the only way to get the timestamp out of a <code>Date</code> object. A noncoercion approach is perhaps even preferable, as it&#39;s even more explicit:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> timestamp <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Date</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getTime</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// var timestamp = (new Date()).getTime();</span>
<span class="token comment">// var timestamp = (new Date).getTime();</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>But an <em>even more</em> preferable noncoercion option is to use the ES5 added <code>Date.now()</code> static function:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> timestamp <span class="token operator">=</span> Date<span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>And if you want to polyfill <code>Date.now()</code> into older browsers, it&#39;s pretty simple:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>Date<span class="token punctuation">.</span>now<span class="token punctuation">)</span> <span class="token punctuation">{</span>
	Date<span class="token punctuation">.</span><span class="token function-variable function">now</span> <span class="token operator">=</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
		<span class="token keyword">return</span> <span class="token operator">+</span><span class="token keyword">new</span> <span class="token class-name">Date</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
	<span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>I&#39;d recommend skipping the coercion forms related to dates. Use <code>Date.now()</code> for current <em>now</em> timestamps, and <code>new Date( .. ).getTime()</code> for getting a timestamp of a specific <em>non-now</em> date/time that you need to specify.</p><h4 id="the-curious-case-of-the" tabindex="-1"><a class="header-anchor" href="#the-curious-case-of-the" aria-hidden="true">#</a> The Curious Case of the <code>~</code></h4><p>One coercive JS operator that is often overlooked and usually very confused is the tilde <code>~</code> operator (aka &quot;bitwise NOT&quot;). Many of those who even understand what it does will often times still want to avoid it. But sticking to the spirit of our approach in this book and series, let&#39;s dig into it to find out if <code>~</code> has anything useful to give us.</p><p>In the &quot;32-bit (Signed) Integers&quot; section of Chapter 2, we covered how bitwise operators in JS are defined only for 32-bit operations, which means they force their operands to conform to 32-bit value representations. The rules for how this happens are controlled by the <code>ToInt32</code> abstract operation (ES5 spec, section 9.5).</p><p><code>ToInt32</code> first does a <code>ToNumber</code> coercion, which means if the value is <code>&quot;123&quot;</code>, it&#39;s going to first become <code>123</code> before the <code>ToInt32</code> rules are applied.</p><p>While not <em>technically</em> coercion itself (since the type doesn&#39;t change!), using bitwise operators (like <code>|</code> or <code>~</code>) with certain special <code>number</code> values produces a coercive effect that results in a different <code>number</code> value.</p><p>For example, let&#39;s first consider the <code>|</code> &quot;bitwise OR&quot; operator used in the otherwise no-op idiom <code>0 | x</code>, which (as Chapter 2 showed) essentially only does the <code>ToInt32</code> conversion:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token number">0</span> <span class="token operator">|</span> <span class="token operator">-</span><span class="token number">0</span><span class="token punctuation">;</span>			<span class="token comment">// 0</span>
<span class="token number">0</span> <span class="token operator">|</span> <span class="token number">NaN</span><span class="token punctuation">;</span>		<span class="token comment">// 0</span>
<span class="token number">0</span> <span class="token operator">|</span> <span class="token number">Infinity</span><span class="token punctuation">;</span>	<span class="token comment">// 0</span>
<span class="token number">0</span> <span class="token operator">|</span> <span class="token operator">-</span><span class="token number">Infinity</span><span class="token punctuation">;</span>	<span class="token comment">// 0</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>These special numbers aren&#39;t 32-bit representable (since they come from the 64-bit IEEE 754 standard -- see Chapter 2), so <code>ToInt32</code> just specifies <code>0</code> as the result from these values.</p><p>It&#39;s debatable if <code>0 | __</code> is an <em>explicit</em> form of this coercive <code>ToInt32</code> operation or if it&#39;s more <em>implicit</em>. From the spec perspective, it&#39;s unquestionably <em>explicit</em>, but if you don&#39;t understand bitwise operations at this level, it can seem a bit more <em>implicitly</em> magical. Nevertheless, consistent with other assertions in this chapter, we will call it <em>explicit</em>.</p><p>So, let&#39;s turn our attention back to <code>~</code>. The <code>~</code> operator first &quot;coerces&quot; to a 32-bit <code>number</code> value, and then performs a bitwise negation (flipping each bit&#39;s parity).</p><p><strong>Note:</strong> This is very similar to how <code>!</code> not only coerces its value to <code>boolean</code> but also flips its parity (see discussion of the &quot;unary <code>!</code>&quot; later).</p><p>But... what!? Why do we care about bits being flipped? That&#39;s some pretty specialized, nuanced stuff. It&#39;s pretty rare for JS developers to need to reason about individual bits.</p><p>Another way of thinking about the definition of <code>~</code> comes from old-school computer science/discrete Mathematics: <code>~</code> performs two&#39;s-complement. Great, thanks, that&#39;s totally clearer!</p><p>Let&#39;s try again: <code>~x</code> is roughly the same as <code>-(x+1)</code>. That&#39;s weird, but slightly easier to reason about. So:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token operator">~</span><span class="token number">42</span><span class="token punctuation">;</span>	<span class="token comment">// -(42+1) ==&gt; -43</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>You&#39;re probably still wondering what the heck all this <code>~</code> stuff is about, or why it really matters for a coercion discussion. Let&#39;s quickly get to the point.</p><p>Consider <code>-(x+1)</code>. What&#39;s the only value that you can perform that operation on that will produce a <code>0</code> (or <code>-0</code> technically!) result? <code>-1</code>. In other words, <code>~</code> used with a range of <code>number</code> values will produce a falsy (easily coercible to <code>false</code>) <code>0</code> value for the <code>-1</code> input value, and any other truthy <code>number</code> otherwise.</p><p>Why is that relevant?</p><p><code>-1</code> is commonly called a &quot;sentinel value,&quot; which basically means a value that&#39;s given an arbitrary semantic meaning within the greater set of values of its same type (<code>number</code>s). The C-language uses <code>-1</code> sentinel values for many functions that return <code>&gt;= 0</code> values for &quot;success&quot; and <code>-1</code> for &quot;failure.&quot;</p><p>JavaScript adopted this precedent when defining the <code>string</code> operation <code>indexOf(..)</code>, which searches for a substring and if found returns its zero-based index position, or <code>-1</code> if not found.</p><p>It&#39;s pretty common to try to use <code>indexOf(..)</code> not just as an operation to get the position, but as a <code>boolean</code> check of presence/absence of a substring in another <code>string</code>. Here&#39;s how developers usually perform such checks:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;Hello World&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>a<span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span> <span class="token string">&quot;lo&quot;</span> <span class="token punctuation">)</span> <span class="token operator">&gt;=</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>	<span class="token comment">// true</span>
	<span class="token comment">// found it!</span>
<span class="token punctuation">}</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>a<span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span> <span class="token string">&quot;lo&quot;</span> <span class="token punctuation">)</span> <span class="token operator">!=</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>	<span class="token comment">// true</span>
	<span class="token comment">// found it</span>
<span class="token punctuation">}</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>a<span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span> <span class="token string">&quot;ol&quot;</span> <span class="token punctuation">)</span> <span class="token operator">&lt;</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>	<span class="token comment">// true</span>
	<span class="token comment">// not found!</span>
<span class="token punctuation">}</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>a<span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span> <span class="token string">&quot;ol&quot;</span> <span class="token punctuation">)</span> <span class="token operator">==</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>	<span class="token comment">// true</span>
	<span class="token comment">// not found!</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br></div></div><p>I find it kind of gross to look at <code>&gt;= 0</code> or <code>== -1</code>. It&#39;s basically a &quot;leaky abstraction,&quot; in that it&#39;s leaking underlying implementation behavior -- the usage of sentinel <code>-1</code> for &quot;failure&quot; -- into my code. I would prefer to hide such a detail.</p><p>And now, finally, we see why <code>~</code> could help us! Using <code>~</code> with <code>indexOf()</code> &quot;coerces&quot; (actually just transforms) the value <strong>to be appropriately <code>boolean</code>-coercible</strong>:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;Hello World&quot;</span><span class="token punctuation">;</span>

<span class="token operator">~</span>a<span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span> <span class="token string">&quot;lo&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>			<span class="token comment">// -4   &lt;-- truthy!</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">~</span>a<span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span> <span class="token string">&quot;lo&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>	<span class="token comment">// true</span>
	<span class="token comment">// found it!</span>
<span class="token punctuation">}</span>

<span class="token operator">~</span>a<span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span> <span class="token string">&quot;ol&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>			<span class="token comment">// 0    &lt;-- falsy!</span>
<span class="token operator">!</span><span class="token operator">~</span>a<span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span> <span class="token string">&quot;ol&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// true</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span><span class="token operator">~</span>a<span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span> <span class="token string">&quot;ol&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>	<span class="token comment">// true</span>
	<span class="token comment">// not found!</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p><code>~</code> takes the return value of <code>indexOf(..)</code> and transforms it: for the &quot;failure&quot; <code>-1</code> we get the falsy <code>0</code>, and every other value is truthy.</p><p><strong>Note:</strong> The <code>-(x+1)</code> pseudo-algorithm for <code>~</code> would imply that <code>~-1</code> is <code>-0</code>, but actually it produces <code>0</code> because the underlying operation is actually bitwise, not mathematic.</p><p>Technically, <code>if (~a.indexOf(..))</code> is still relying on <em>implicit</em> coercion of its resultant <code>0</code> to <code>false</code> or nonzero to <code>true</code>. But overall, <code>~</code> still feels to me more like an <em>explicit</em> coercion mechanism, as long as you know what it&#39;s intended to do in this idiom.</p><p>I find this to be cleaner code than the previous <code>&gt;= 0</code> / <code>== -1</code> clutter.</p><h5 id="truncating-bits" tabindex="-1"><a class="header-anchor" href="#truncating-bits" aria-hidden="true">#</a> Truncating Bits</h5><p>There&#39;s one more place <code>~</code> may show up in code you run across: some developers use the double tilde <code>~~</code> to truncate the decimal part of a <code>number</code> (i.e., &quot;coerce&quot; it to a whole number &quot;integer&quot;). It&#39;s commonly (though mistakingly) said this is the same result as calling <code>Math.floor(..)</code>.</p><p>How <code>~~</code> works is that the first <code>~</code> applies the <code>ToInt32</code> &quot;coercion&quot; and does the bitwise flip, and then the second <code>~</code> does another bitwise flip, flipping all the bits back to the original state. The end result is just the <code>ToInt32</code> &quot;coercion&quot; (aka truncation).</p><p><strong>Note:</strong> The bitwise double-flip of <code>~~</code> is very similar to the parity double-negate <code>!!</code> behavior, explained in the &quot;Explicitly: * --&gt; Boolean&quot; section later.</p><p>However, <code>~~</code> needs some caution/clarification. First, it only works reliably on 32-bit values. But more importantly, it doesn&#39;t work the same on negative numbers as <code>Math.floor(..)</code> does!</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code>Math<span class="token punctuation">.</span><span class="token function">floor</span><span class="token punctuation">(</span> <span class="token operator">-</span><span class="token number">49.6</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// -50</span>
<span class="token operator">~</span><span class="token operator">~</span><span class="token operator">-</span><span class="token number">49.6</span><span class="token punctuation">;</span>				<span class="token comment">// -49</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Setting the <code>Math.floor(..)</code> difference aside, <code>~~x</code> can truncate to a (32-bit) integer. But so does <code>x | 0</code>, and seemingly with (slightly) <em>less effort</em>.</p><p>So, why might you choose <code>~~x</code> over <code>x | 0</code>, then? Operator precedence (see Chapter 5):</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token operator">~</span><span class="token operator">~</span><span class="token number">1E20</span> <span class="token operator">/</span> <span class="token number">10</span><span class="token punctuation">;</span>		<span class="token comment">// 166199296</span>

<span class="token number">1E20</span> <span class="token operator">|</span> <span class="token number">0</span> <span class="token operator">/</span> <span class="token number">10</span><span class="token punctuation">;</span>		<span class="token comment">// 1661992960</span>
<span class="token punctuation">(</span><span class="token number">1E20</span> <span class="token operator">|</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token operator">/</span> <span class="token number">10</span><span class="token punctuation">;</span>	<span class="token comment">// 166199296</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Just as with all other advice here, use <code>~</code> and <code>~~</code> as explicit mechanisms for &quot;coercion&quot; and value transformation only if everyone who reads/writes such code is properly aware of how these operators work!</p><h3 id="explicitly-parsing-numeric-strings" tabindex="-1"><a class="header-anchor" href="#explicitly-parsing-numeric-strings" aria-hidden="true">#</a> Explicitly: Parsing Numeric Strings</h3><p>A similar outcome to coercing a <code>string</code> to a <code>number</code> can be achieved by parsing a <code>number</code> out of a <code>string</code>&#39;s character contents. There are, however, distinct differences between this parsing and the type conversion we examined above.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token string">&quot;42px&quot;</span><span class="token punctuation">;</span>

<span class="token function">Number</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// 42</span>
<span class="token function">parseInt</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// 42</span>

<span class="token function">Number</span><span class="token punctuation">(</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// NaN</span>
<span class="token function">parseInt</span><span class="token punctuation">(</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// 42</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Parsing a numeric value out of a string is <em>tolerant</em> of non-numeric characters -- it just stops parsing left-to-right when encountered -- whereas coercion is <em>not tolerant</em> and fails resulting in the <code>NaN</code> value.</p><p>Parsing should not be seen as a substitute for coercion. These two tasks, while similar, have different purposes. Parse a <code>string</code> as a <code>number</code> when you don&#39;t know/care what other non-numeric characters there may be on the right-hand side. Coerce a <code>string</code> (to a <code>number</code>) when the only acceptable values are numeric and something like <code>&quot;42px&quot;</code> should be rejected as a <code>number</code>.</p><p><strong>Tip:</strong> <code>parseInt(..)</code> has a twin, <code>parseFloat(..)</code>, which (as it sounds) pulls out a floating-point number from a string.</p><p>Don&#39;t forget that <code>parseInt(..)</code> operates on <code>string</code> values. It makes absolutely no sense to pass a <code>number</code> value to <code>parseInt(..)</code>. Nor would it make sense to pass any other type of value, like <code>true</code>, <code>function(){..}</code> or <code>[1,2,3]</code>.</p><p>If you pass a non-<code>string</code>, the value you pass will automatically be coerced to a <code>string</code> first (see &quot;<code>ToString</code>&quot; earlier), which would clearly be a kind of hidden <em>implicit</em> coercion. It&#39;s a really bad idea to rely upon such a behavior in your program, so never use <code>parseInt(..)</code> with a non-<code>string</code> value.</p><p>Prior to ES5, another gotcha existed with <code>parseInt(..)</code>, which was the source of many JS programs&#39; bugs. If you didn&#39;t pass a second argument to indicate which numeric base (aka radix) to use for interpreting the numeric <code>string</code> contents, <code>parseInt(..)</code> would look at the beginning character(s) to make a guess.</p><p>If the first two characters were <code>&quot;0x&quot;</code> or <code>&quot;0X&quot;</code>, the guess (by convention) was that you wanted to interpret the <code>string</code> as a hexadecimal (base-16) <code>number</code>. Otherwise, if the first character was <code>&quot;0&quot;</code>, the guess (again, by convention) was that you wanted to interpret the <code>string</code> as an octal (base-8) <code>number</code>.</p><p>Hexadecimal <code>string</code>s (with the leading <code>0x</code> or <code>0X</code>) aren&#39;t terribly easy to get mixed up. But the octal number guessing proved devilishly common. For example:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> hour <span class="token operator">=</span> <span class="token function">parseInt</span><span class="token punctuation">(</span> selectedHour<span class="token punctuation">.</span>value <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> minute <span class="token operator">=</span> <span class="token function">parseInt</span><span class="token punctuation">(</span> selectedMinute<span class="token punctuation">.</span>value <span class="token punctuation">)</span><span class="token punctuation">;</span>

console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> <span class="token string">&quot;The time you selected was: &quot;</span> <span class="token operator">+</span> hour <span class="token operator">+</span> <span class="token string">&quot;:&quot;</span> <span class="token operator">+</span> minute<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Seems harmless, right? Try selecting <code>08</code> for the hour and <code>09</code> for the minute. You&#39;ll get <code>0:0</code>. Why? because neither <code>8</code> nor <code>9</code> are valid characters in octal base-8.</p><p>The pre-ES5 fix was simple, but so easy to forget: <strong>always pass <code>10</code> as the second argument</strong>. This was totally safe:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> hour <span class="token operator">=</span> <span class="token function">parseInt</span><span class="token punctuation">(</span> selectedHour<span class="token punctuation">.</span>value<span class="token punctuation">,</span> <span class="token number">10</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> minute <span class="token operator">=</span> <span class="token function">parseInt</span><span class="token punctuation">(</span> selectedMiniute<span class="token punctuation">.</span>value<span class="token punctuation">,</span> <span class="token number">10</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>As of ES5, <code>parseInt(..)</code> no longer guesses octal. Unless you say otherwise, it assumes base-10 (or base-16 for <code>&quot;0x&quot;</code> prefixes). That&#39;s much nicer. Just be careful if your code has to run in pre-ES5 environments, in which case you still need to pass <code>10</code> for the radix.</p><h4 id="parsing-non-strings" tabindex="-1"><a class="header-anchor" href="#parsing-non-strings" aria-hidden="true">#</a> Parsing Non-Strings</h4><p>One somewhat infamous example of <code>parseInt(..)</code>&#39;s behavior is highlighted in a sarcastic joke post a few years ago, poking fun at this JS behavior:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token function">parseInt</span><span class="token punctuation">(</span> <span class="token number">1</span><span class="token operator">/</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">19</span> <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 18</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The assumptive (but totally invalid) assertion was, &quot;If I pass in Infinity, and parse an integer out of that, I should get Infinity back, not 18.&quot; Surely, JS must be crazy for this outcome, right?</p><p>Though this example is obviously contrived and unreal, let&#39;s indulge the madness for a moment and examine whether JS really is that crazy.</p><p>First off, the most obvious sin committed here is to pass a non-<code>string</code> to <code>parseInt(..)</code>. That&#39;s a no-no. Do it and you&#39;re asking for trouble. But even if you do, JS politely coerces what you pass in into a <code>string</code> that it can try to parse.</p><p>Some would argue that this is unreasonable behavior, and that <code>parseInt(..)</code> should refuse to operate on a non-<code>string</code> value. Should it perhaps throw an error? That would be very Java-like, frankly. I shudder at thinking JS should start throwing errors all over the place so that <code>try..catch</code> is needed around almost every line.</p><p>Should it return <code>NaN</code>? Maybe. But... what about:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token function">parseInt</span><span class="token punctuation">(</span> <span class="token keyword">new</span> <span class="token class-name">String</span><span class="token punctuation">(</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Should that fail, too? It&#39;s a non-<code>string</code> value. If you want that <code>String</code> object wrapper to be unboxed to <code>&quot;42&quot;</code>, then is it really so unusual for <code>42</code> to first become <code>&quot;42&quot;</code> so that <code>42</code> can be parsed back out?</p><p>I would argue that this half-<em>explicit</em>, half-<em>implicit</em> coercion that can occur can often be a very helpful thing. For example:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">{</span>
	<span class="token literal-property property">num</span><span class="token operator">:</span> <span class="token number">21</span><span class="token punctuation">,</span>
	<span class="token function-variable function">toString</span><span class="token operator">:</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token keyword">return</span> <span class="token function">String</span><span class="token punctuation">(</span> <span class="token keyword">this</span><span class="token punctuation">.</span>num <span class="token operator">*</span> <span class="token number">2</span> <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token function">parseInt</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 42</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>The fact that <code>parseInt(..)</code> forcibly coerces its value to a <code>string</code> to perform the parse on is quite sensible. If you pass in garbage, and you get garbage back out, don&#39;t blame the trash can -- it just did its job faithfully.</p><p>So, if you pass in a value like <code>Infinity</code> (the result of <code>1 / 0</code> obviously), what sort of <code>string</code> representation would make the most sense for its coercion? Only two reasonable choices come to mind: <code>&quot;Infinity&quot;</code> and <code>&quot;\u221E&quot;</code>. JS chose <code>&quot;Infinity&quot;</code>. I&#39;m glad it did.</p><p>I think it&#39;s a good thing that <strong>all values</strong> in JS have some sort of default <code>string</code> representation, so that they aren&#39;t mysterious black boxes that we can&#39;t debug and reason about.</p><p>Now, what about base-19? Obviously, completely bogus and contrived. No real JS programs use base-19. It&#39;s absurd. But again, let&#39;s indulge the ridiculousness. In base-19, the valid numeric characters are <code>0</code> - <code>9</code> and <code>a</code> - <code>i</code> (case insensitive).</p><p>So, back to our <code>parseInt( 1/0, 19 )</code> example. It&#39;s essentially <code>parseInt( &quot;Infinity&quot;, 19 )</code>. How does it parse? The first character is <code>&quot;I&quot;</code>, which is value <code>18</code> in the silly base-19. The second character <code>&quot;n&quot;</code> is not in the valid set of numeric characters, and as such the parsing simply politely stops, just like when it ran across <code>&quot;p&quot;</code> in <code>&quot;42px&quot;</code>.</p><p>The result? <code>18</code>. Exactly like it sensibly should be. The behaviors involved to get us there, and not to an error or to <code>Infinity</code> itself, are <strong>very important</strong> to JS, and should not be so easily discarded.</p><p>Other examples of this behavior with <code>parseInt(..)</code> that may be surprising but are quite sensible include:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token function">parseInt</span><span class="token punctuation">(</span> <span class="token number">0.000008</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// 0   (&quot;0&quot; from &quot;0.000008&quot;)</span>
<span class="token function">parseInt</span><span class="token punctuation">(</span> <span class="token number">0.0000008</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// 8   (&quot;8&quot; from &quot;8e-7&quot;)</span>
<span class="token function">parseInt</span><span class="token punctuation">(</span> <span class="token boolean">false</span><span class="token punctuation">,</span> <span class="token number">16</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// 250 (&quot;fa&quot; from &quot;false&quot;)</span>
<span class="token function">parseInt</span><span class="token punctuation">(</span> parseInt<span class="token punctuation">,</span> <span class="token number">16</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// 15  (&quot;f&quot; from &quot;function..&quot;)</span>

<span class="token function">parseInt</span><span class="token punctuation">(</span> <span class="token string">&quot;0x10&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>			<span class="token comment">// 16</span>
<span class="token function">parseInt</span><span class="token punctuation">(</span> <span class="token string">&quot;103&quot;</span><span class="token punctuation">,</span> <span class="token number">2</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// 2</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><code>parseInt(..)</code> is actually pretty predictable and consistent in its behavior. If you use it correctly, you&#39;ll get sensible results. If you use it incorrectly, the crazy results you get are not the fault of JavaScript.</p><h3 id="explicitly-boolean" tabindex="-1"><a class="header-anchor" href="#explicitly-boolean" aria-hidden="true">#</a> Explicitly: * --&gt; Boolean</h3><p>Now, let&#39;s examine coercing from any non-<code>boolean</code> value to a <code>boolean</code>.</p><p>Just like with <code>String(..)</code> and <code>Number(..)</code> above, <code>Boolean(..)</code> (without the <code>new</code>, of course!) is an explicit way of forcing the <code>ToBoolean</code> coercion:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;0&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> e <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> f <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> g<span class="token punctuation">;</span>

<span class="token function">Boolean</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// true</span>
<span class="token function">Boolean</span><span class="token punctuation">(</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// true</span>
<span class="token function">Boolean</span><span class="token punctuation">(</span> c <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// true</span>

<span class="token function">Boolean</span><span class="token punctuation">(</span> d <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// false</span>
<span class="token function">Boolean</span><span class="token punctuation">(</span> e <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// false</span>
<span class="token function">Boolean</span><span class="token punctuation">(</span> f <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// false</span>
<span class="token function">Boolean</span><span class="token punctuation">(</span> g <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><p>While <code>Boolean(..)</code> is clearly explicit, it&#39;s not at all common or idiomatic.</p><p>Just like the unary <code>+</code> operator coerces a value to a <code>number</code> (see above), the unary <code>!</code> negate operator explicitly coerces a value to a <code>boolean</code>. The <em>problem</em> is that it also flips the value from truthy to falsy or vice versa. So, the most common way JS developers explicitly coerce to <code>boolean</code> is to use the <code>!!</code> double-negate operator, because the second <code>!</code> will flip the parity back to the original:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;0&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> e <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> f <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> g<span class="token punctuation">;</span>

<span class="token operator">!</span><span class="token operator">!</span>a<span class="token punctuation">;</span>	<span class="token comment">// true</span>
<span class="token operator">!</span><span class="token operator">!</span>b<span class="token punctuation">;</span>	<span class="token comment">// true</span>
<span class="token operator">!</span><span class="token operator">!</span>c<span class="token punctuation">;</span>	<span class="token comment">// true</span>

<span class="token operator">!</span><span class="token operator">!</span>d<span class="token punctuation">;</span>	<span class="token comment">// false</span>
<span class="token operator">!</span><span class="token operator">!</span>e<span class="token punctuation">;</span>	<span class="token comment">// false</span>
<span class="token operator">!</span><span class="token operator">!</span>f<span class="token punctuation">;</span>	<span class="token comment">// false</span>
<span class="token operator">!</span><span class="token operator">!</span>g<span class="token punctuation">;</span>	<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><p>Any of these <code>ToBoolean</code> coercions would happen <em>implicitly</em> without the <code>Boolean(..)</code> or <code>!!</code>, if used in a <code>boolean</code> context such as an <code>if (..) ..</code> statement. But the goal here is to explicitly force the value to a <code>boolean</code> to make it clearer that the <code>ToBoolean</code> coercion is intended.</p><p>Another example use-case for explicit <code>ToBoolean</code> coercion is if you want to force a <code>true</code>/<code>false</code> value coercion in the JSON serialization of a data structure:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">[</span>
	<span class="token number">1</span><span class="token punctuation">,</span>
	<span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span> <span class="token comment">/*..*/</span> <span class="token punctuation">}</span><span class="token punctuation">,</span>
	<span class="token number">2</span><span class="token punctuation">,</span>
	<span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span> <span class="token comment">/*..*/</span> <span class="token punctuation">}</span>
<span class="token punctuation">]</span><span class="token punctuation">;</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;[1,null,2,null]&quot;</span>

<span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span> a<span class="token punctuation">,</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token parameter">key<span class="token punctuation">,</span>val</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
	<span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token keyword">typeof</span> val <span class="token operator">==</span> <span class="token string">&quot;function&quot;</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
		<span class="token comment">// force \`ToBoolean\` coercion of the function</span>
		<span class="token keyword">return</span> <span class="token operator">!</span><span class="token operator">!</span>val<span class="token punctuation">;</span>
	<span class="token punctuation">}</span>
	<span class="token keyword">else</span> <span class="token punctuation">{</span>
		<span class="token keyword">return</span> val<span class="token punctuation">;</span>
	<span class="token punctuation">}</span>
<span class="token punctuation">}</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// &quot;[1,true,2,true]&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>If you come to JavaScript from Java, you may recognize this idiom:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> b <span class="token operator">=</span> a <span class="token operator">?</span> <span class="token boolean">true</span> <span class="token operator">:</span> <span class="token boolean">false</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>The <code>? :</code> ternary operator will test <code>a</code> for truthiness, and based on that test will either assign <code>true</code> or <code>false</code> to <code>b</code>, accordingly.</p><p>On its surface, this idiom looks like a form of <em>explicit</em> <code>ToBoolean</code>-type coercion, since it&#39;s obvious that only either <code>true</code> or <code>false</code> come out of the operation.</p><p>However, there&#39;s a hidden <em>implicit</em> coercion, in that the <code>a</code> expression has to first be coerced to <code>boolean</code> to perform the truthiness test. I&#39;d call this idiom &quot;explicitly implicit.&quot; Furthermore, I&#39;d suggest <strong>you should avoid this idiom completely</strong> in JavaScript. It offers no real benefit, and worse, masquerades as something it&#39;s not.</p><p><code>Boolean(a)</code> and <code>!!a</code> are far better as <em>explicit</em> coercion options.</p><h2 id="implicit-coercion" tabindex="-1"><a class="header-anchor" href="#implicit-coercion" aria-hidden="true">#</a> Implicit Coercion</h2><p><em>Implicit</em> coercion refers to type conversions that are hidden, with non-obvious side-effects that implicitly occur from other actions. In other words, <em>implicit coercions</em> are any type conversions that aren&#39;t obvious (to you).</p><p>While it&#39;s clear what the goal of <em>explicit</em> coercion is (making code explicit and more understandable), it might be <em>too</em> obvious that <em>implicit</em> coercion has the opposite goal: making code harder to understand.</p><p>Taken at face value, I believe that&#39;s where much of the ire towards coercion comes from. The majority of complaints about &quot;JavaScript coercion&quot; are actually aimed (whether they realize it or not) at <em>implicit</em> coercion.</p><p><strong>Note:</strong> Douglas Crockford, author of <em>&quot;JavaScript: The Good Parts&quot;</em>, has claimed in many conference talks and writings that JavaScript coercion should be avoided. But what he seems to mean is that <em>implicit</em> coercion is bad (in his opinion). However, if you read his own code, you&#39;ll find plenty of examples of coercion, both <em>implicit</em> and <em>explicit</em>! In truth, his angst seems to primarily be directed at the <code>==</code> operation, but as you&#39;ll see in this chapter, that&#39;s only part of the coercion mechanism.</p><p>So, <strong>is implicit coercion</strong> evil? Is it dangerous? Is it a flaw in JavaScript&#39;s design? Should we avoid it at all costs?</p><p>I bet most of you readers are inclined to enthusiastically cheer, &quot;Yes!&quot;</p><p><strong>Not so fast.</strong> Hear me out.</p><p>Let&#39;s take a different perspective on what <em>implicit</em> coercion is, and can be, than just that it&#39;s &quot;the opposite of the good explicit kind of coercion.&quot; That&#39;s far too narrow and misses an important nuance.</p><p>Let&#39;s define the goal of <em>implicit</em> coercion as: to reduce verbosity, boilerplate, and/or unnecessary implementation detail that clutters up our code with noise that distracts from the more important intent.</p><h3 id="simplifying-implicitly" tabindex="-1"><a class="header-anchor" href="#simplifying-implicitly" aria-hidden="true">#</a> Simplifying Implicitly</h3><p>Before we even get to JavaScript, let me suggest something pseudo-code&#39;ish from some theoretical strongly typed language to illustrate:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code>SomeType x <span class="token operator">=</span> <span class="token function">SomeType</span><span class="token punctuation">(</span> <span class="token function">AnotherType</span><span class="token punctuation">(</span> y <span class="token punctuation">)</span> <span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>In this example, I have some arbitrary type of value in <code>y</code> that I want to convert to the <code>SomeType</code> type. The problem is, this language can&#39;t go directly from whatever <code>y</code> currently is to <code>SomeType</code>. It needs an intermediate step, where it first converts to <code>AnotherType</code>, and then from <code>AnotherType</code> to <code>SomeType</code>.</p><p>Now, what if that language (or definition you could create yourself with the language) <em>did</em> just let you say:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code>SomeType x <span class="token operator">=</span> <span class="token function">SomeType</span><span class="token punctuation">(</span> y <span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Wouldn&#39;t you generally agree that we simplified the type conversion here to reduce the unnecessary &quot;noise&quot; of the intermediate conversion step? I mean, is it <em>really</em> all that important, right here at this point in the code, to see and deal with the fact that <code>y</code> goes to <code>AnotherType</code> first before then going to <code>SomeType</code>?</p><p>Some would argue, at least in some circumstances, yes. But I think an equal argument can be made of many other circumstances that here, the simplification <strong>actually aids in the readability of the code</strong> by abstracting or hiding away such details, either in the language itself or in our own abstractions.</p><p>Undoubtedly, behind the scenes, somewhere, the intermediate conversion step is still happening. But if that detail is hidden from view here, we can just reason about getting <code>y</code> to type <code>SomeType</code> as a generic operation and hide the messy details.</p><p>While not a perfect analogy, what I&#39;m going to argue throughout the rest of this chapter is that JS <em>implicit</em> coercion can be thought of as providing a similar aid to your code.</p><p>But, <strong>and this is very important</strong>, that is not an unbounded, absolute statement. There are definitely plenty of <em>evils</em> lurking around <em>implicit</em> coercion, that will harm your code much more than any potential readability improvements. Clearly, we have to learn how to avoid such constructs so we don&#39;t poison our code with all manner of bugs.</p><p>Many developers believe that if a mechanism can do some useful thing <strong>A</strong> but can also be abused or misused to do some awful thing <strong>Z</strong>, then we should throw out that mechanism altogether, just to be safe.</p><p>My encouragement to you is: don&#39;t settle for that. Don&#39;t &quot;throw the baby out with the bathwater.&quot; Don&#39;t assume <em>implicit</em> coercion is all bad because all you think you&#39;ve ever seen is its &quot;bad parts.&quot; I think there are &quot;good parts&quot; here, and I want to help and inspire more of you to find and embrace them!</p><h3 id="implicitly-strings-numbers" tabindex="-1"><a class="header-anchor" href="#implicitly-strings-numbers" aria-hidden="true">#</a> Implicitly: Strings &lt;--&gt; Numbers</h3><p>Earlier in this chapter, we explored <em>explicitly</em> coercing between <code>string</code> and <code>number</code> values. Now, let&#39;s explore the same task but with <em>implicit</em> coercion approaches. But before we do, we have to examine some nuances of operations that will <em>implicitly</em> force coercion.</p><p>The <code>+</code> operator is overloaded to serve the purposes of both <code>number</code> addition and <code>string</code> concatenation. So how does JS know which type of operation you want to use? Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token string">&quot;0&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>

a <span class="token operator">+</span> b<span class="token punctuation">;</span> <span class="token comment">// &quot;420&quot;</span>
c <span class="token operator">+</span> d<span class="token punctuation">;</span> <span class="token comment">// 42</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>What&#39;s different that causes <code>&quot;420&quot;</code> vs <code>42</code>? It&#39;s a common misconception that the difference is whether one or both of the operands is a <code>string</code>, as that means <code>+</code> will assume <code>string</code> concatenation. While that&#39;s partially true, it&#39;s more complicated than that.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">3</span><span class="token punctuation">,</span><span class="token number">4</span><span class="token punctuation">]</span><span class="token punctuation">;</span>

a <span class="token operator">+</span> b<span class="token punctuation">;</span> <span class="token comment">// &quot;1,23,4&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Neither of these operands is a <code>string</code>, but clearly they were both coerced to <code>string</code>s and then the <code>string</code> concatenation kicked in. So what&#39;s really going on?</p><p>(<strong>Warning:</strong> deeply nitty gritty spec-speak coming, so skip the next two paragraphs if that intimidates you!)</p><hr><p>According to ES5 spec section 11.6.1, the <code>+</code> algorithm (when an <code>object</code> value is an operand) will concatenate if either operand is either already a <code>string</code>, or if the following steps produce a <code>string</code> representation. So, when <code>+</code> receives an <code>object</code> (including <code>array</code>) for either operand, it first calls the <code>ToPrimitive</code> abstract operation (section 9.1) on the value, which then calls the <code>[[DefaultValue]]</code> algorithm (section 8.12.8) with a context hint of <code>number</code>.</p><p>If you&#39;re paying close attention, you&#39;ll notice that this operation is now identical to how the <code>ToNumber</code> abstract operation handles <code>object</code>s (see the &quot;<code>ToNumber</code>&quot;&quot; section earlier). The <code>valueOf()</code> operation on the <code>array</code> will fail to produce a simple primitive, so it then falls to a <code>toString()</code> representation. The two <code>array</code>s thus become <code>&quot;1,2&quot;</code> and <code>&quot;3,4&quot;</code>, respectively. Now, <code>+</code> concatenates the two <code>string</code>s as you&#39;d normally expect: <code>&quot;1,23,4&quot;</code>.</p><hr><p>Let&#39;s set aside those messy details and go back to an earlier, simplified explanation: if either operand to <code>+</code> is a <code>string</code> (or becomes one with the above steps!), the operation will be <code>string</code> concatenation. Otherwise, it&#39;s always numeric addition.</p><p><strong>Note:</strong> A commonly cited coercion gotcha is <code>[] + {}</code> vs. <code>{} + []</code>, as those two expressions result, respectively, in <code>&quot;[object Object]&quot;</code> and <code>0</code>. There&#39;s more to it, though, and we cover those details in &quot;Blocks&quot; in Chapter 5.</p><p>What&#39;s that mean for <em>implicit</em> coercion?</p><p>You can coerce a <code>number</code> to a <code>string</code> simply by &quot;adding&quot; the <code>number</code> and the <code>&quot;&quot;</code> empty <code>string</code>:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> a <span class="token operator">+</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>

b<span class="token punctuation">;</span> <span class="token comment">// &quot;42&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><strong>Tip:</strong> Numeric addition with the <code>+</code> operator is commutative, which means <code>2 + 3</code> is the same as <code>3 + 2</code>. String concatenation with <code>+</code> is obviously not generally commutative, <strong>but</strong> with the specific case of <code>&quot;&quot;</code>, it&#39;s effectively commutative, as <code>a + &quot;&quot;</code> and <code>&quot;&quot; + a</code> will produce the same result.</p><p>It&#39;s extremely common/idiomatic to (<em>implicitly</em>) coerce <code>number</code> to <code>string</code> with a <code>+ &quot;&quot;</code> operation. In fact, interestingly, even some of the most vocal critics of <em>implicit</em> coercion still use that approach in their own code, instead of one of its <em>explicit</em> alternatives.</p><p><strong>I think this is a great example</strong> of a useful form in <em>implicit</em> coercion, despite how frequently the mechanism gets criticized!</p><p>Comparing this <em>implicit</em> coercion of <code>a + &quot;&quot;</code> to our earlier example of <code>String(a)</code> <em>explicit</em> coercion, there&#39;s one additional quirk to be aware of. Because of how the <code>ToPrimitive</code> abstract operation works, <code>a + &quot;&quot;</code> invokes <code>valueOf()</code> on the <code>a</code> value, whose return value is then finally converted to a <code>string</code> via the internal <code>ToString</code> abstract operation. But <code>String(a)</code> just invokes <code>toString()</code> directly.</p><p>Both approaches ultimately result in a <code>string</code>, but if you&#39;re using an <code>object</code> instead of a regular primitive <code>number</code> value, you may not necessarily get the <em>same</em> <code>string</code> value!</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">{</span>
	<span class="token function-variable function">valueOf</span><span class="token operator">:</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token keyword">return</span> <span class="token number">42</span><span class="token punctuation">;</span> <span class="token punctuation">}</span><span class="token punctuation">,</span>
	<span class="token function-variable function">toString</span><span class="token operator">:</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token keyword">return</span> <span class="token number">4</span><span class="token punctuation">;</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

a <span class="token operator">+</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>			<span class="token comment">// &quot;42&quot;</span>

<span class="token function">String</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// &quot;4&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Generally, this sort of gotcha won&#39;t bite you unless you&#39;re really trying to create confusing data structures and operations, but you should be careful if you&#39;re defining both your own <code>valueOf()</code> and <code>toString()</code> methods for some <code>object</code>, as how you coerce the value could affect the outcome.</p><p>What about the other direction? How can we <em>implicitly coerce</em> from <code>string</code> to <code>number</code>?</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;3.14&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> a <span class="token operator">-</span> <span class="token number">0</span><span class="token punctuation">;</span>

b<span class="token punctuation">;</span> <span class="token comment">// 3.14</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The <code>-</code> operator is defined only for numeric subtraction, so <code>a - 0</code> forces <code>a</code>&#39;s value to be coerced to a <code>number</code>. While far less common, <code>a * 1</code> or <code>a / 1</code> would accomplish the same result, as those operators are also only defined for numeric operations.</p><p>What about <code>object</code> values with the <code>-</code> operator? Similar story as for <code>+</code> above:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">3</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">;</span>

a <span class="token operator">-</span> b<span class="token punctuation">;</span> <span class="token comment">// 2</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Both <code>array</code> values have to become <code>number</code>s, but they end up first being coerced to <code>strings</code> (using the expected <code>toString()</code> serialization), and then are coerced to <code>number</code>s, for the <code>-</code> subtraction to perform on.</p><p>So, is <em>implicit</em> coercion of <code>string</code> and <code>number</code> values the ugly evil you&#39;ve always heard horror stories about? I don&#39;t personally think so.</p><p>Compare <code>b = String(a)</code> (<em>explicit</em>) to <code>b = a + &quot;&quot;</code> (<em>implicit</em>). I think cases can be made for both approaches being useful in your code. Certainly <code>b = a + &quot;&quot;</code> is quite a bit more common in JS programs, proving its own utility regardless of <em>feelings</em> about the merits or hazards of <em>implicit</em> coercion in general.</p><h3 id="implicitly-booleans-numbers" tabindex="-1"><a class="header-anchor" href="#implicitly-booleans-numbers" aria-hidden="true">#</a> Implicitly: Booleans --&gt; Numbers</h3><p>I think a case where <em>implicit</em> coercion can really shine is in simplifying certain types of complicated <code>boolean</code> logic into simple numeric addition. Of course, this is not a general-purpose technique, but a specific solution for specific cases.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">function</span> <span class="token function">onlyOne</span><span class="token punctuation">(</span><span class="token parameter">a<span class="token punctuation">,</span>b<span class="token punctuation">,</span>c</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token keyword">return</span> <span class="token operator">!</span><span class="token operator">!</span><span class="token punctuation">(</span><span class="token punctuation">(</span>a <span class="token operator">&amp;&amp;</span> <span class="token operator">!</span>b <span class="token operator">&amp;&amp;</span> <span class="token operator">!</span>c<span class="token punctuation">)</span> <span class="token operator">||</span>
		<span class="token punctuation">(</span><span class="token operator">!</span>a <span class="token operator">&amp;&amp;</span> b <span class="token operator">&amp;&amp;</span> <span class="token operator">!</span>c<span class="token punctuation">)</span> <span class="token operator">||</span> <span class="token punctuation">(</span><span class="token operator">!</span>a <span class="token operator">&amp;&amp;</span> <span class="token operator">!</span>b <span class="token operator">&amp;&amp;</span> c<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span>

<span class="token function">onlyOne</span><span class="token punctuation">(</span> a<span class="token punctuation">,</span> b<span class="token punctuation">,</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// true</span>
<span class="token function">onlyOne</span><span class="token punctuation">(</span> b<span class="token punctuation">,</span> a<span class="token punctuation">,</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// true</span>

<span class="token function">onlyOne</span><span class="token punctuation">(</span> a<span class="token punctuation">,</span> b<span class="token punctuation">,</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>This <code>onlyOne(..)</code> utility should only return <code>true</code> if exactly one of the arguments is <code>true</code> / truthy. It&#39;s using <em>implicit</em> coercion on the truthy checks and <em>explicit</em> coercion on the others, including the final return value.</p><p>But what if we needed that utility to be able to handle four, five, or twenty flags in the same way? It&#39;s pretty difficult to imagine implementing code that would handle all those permutations of comparisons.</p><p>But here&#39;s where coercing the <code>boolean</code> values to <code>number</code>s (<code>0</code> or <code>1</code>, obviously) can greatly help:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">function</span> <span class="token function">onlyOne</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token keyword">var</span> sum <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
	<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">var</span> i<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> arguments<span class="token punctuation">.</span>length<span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
		<span class="token comment">// skip falsy values. same as treating</span>
		<span class="token comment">// them as 0&#39;s, but avoids NaN&#39;s.</span>
		<span class="token keyword">if</span> <span class="token punctuation">(</span>arguments<span class="token punctuation">[</span>i<span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
			sum <span class="token operator">+=</span> arguments<span class="token punctuation">[</span>i<span class="token punctuation">]</span><span class="token punctuation">;</span>
		<span class="token punctuation">}</span>
	<span class="token punctuation">}</span>
	<span class="token keyword">return</span> sum <span class="token operator">==</span> <span class="token number">1</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span>

<span class="token function">onlyOne</span><span class="token punctuation">(</span> b<span class="token punctuation">,</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// true</span>
<span class="token function">onlyOne</span><span class="token punctuation">(</span> b<span class="token punctuation">,</span> a<span class="token punctuation">,</span> b<span class="token punctuation">,</span> b<span class="token punctuation">,</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// true</span>

<span class="token function">onlyOne</span><span class="token punctuation">(</span> b<span class="token punctuation">,</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// false</span>
<span class="token function">onlyOne</span><span class="token punctuation">(</span> b<span class="token punctuation">,</span> a<span class="token punctuation">,</span> b<span class="token punctuation">,</span> b<span class="token punctuation">,</span> b<span class="token punctuation">,</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br></div></div><p><strong>Note:</strong> Of course, instead of the <code>for</code> loop in <code>onlyOne(..)</code>, you could more tersely use the ES5 <code>reduce(..)</code> utility, but I didn&#39;t want to obscure the concepts.</p><p>What we&#39;re doing here is relying on the <code>1</code> for <code>true</code>/truthy coercions, and numerically adding them all up. <code>sum += arguments[i]</code> uses <em>implicit</em> coercion to make that happen. If one and only one value in the <code>arguments</code> list is <code>true</code>, then the numeric sum will be <code>1</code>, otherwise the sum will not be <code>1</code> and thus the desired condition is not met.</p><p>We could of course do this with <em>explicit</em> coercion instead:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">function</span> <span class="token function">onlyOne</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token keyword">var</span> sum <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
	<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">var</span> i<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> arguments<span class="token punctuation">.</span>length<span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
		sum <span class="token operator">+=</span> <span class="token function">Number</span><span class="token punctuation">(</span> <span class="token operator">!</span><span class="token operator">!</span>arguments<span class="token punctuation">[</span>i<span class="token punctuation">]</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
	<span class="token punctuation">}</span>
	<span class="token keyword">return</span> sum <span class="token operator">===</span> <span class="token number">1</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>We first use <code>!!arguments[i]</code> to force the coercion of the value to <code>true</code> or <code>false</code>. That&#39;s so you could pass non-<code>boolean</code> values in, like <code>onlyOne( &quot;42&quot;, 0 )</code>, and it would still work as expected (otherwise you&#39;d end up with <code>string</code> concatenation and the logic would be incorrect).</p><p>Once we&#39;re sure it&#39;s a <code>boolean</code>, we do another <em>explicit</em> coercion with <code>Number(..)</code> to make sure the value is <code>0</code> or <code>1</code>.</p><p>Is the <em>explicit</em> coercion form of this utility &quot;better&quot;? It does avoid the <code>NaN</code> trap as explained in the code comments. But, ultimately, it depends on your needs. I personally think the former version, relying on <em>implicit</em> coercion is more elegant (if you won&#39;t be passing <code>undefined</code> or <code>NaN</code>), and the <em>explicit</em> version is needlessly more verbose.</p><p>But as with almost everything we&#39;re discussing here, it&#39;s a judgment call.</p><p><strong>Note:</strong> Regardless of <em>implicit</em> or <em>explicit</em> approaches, you could easily make <code>onlyTwo(..)</code> or <code>onlyFive(..)</code> variations by simply changing the final comparison from <code>1</code>, to <code>2</code> or <code>5</code>, respectively. That&#39;s drastically easier than adding a bunch of <code>&amp;&amp;</code> and <code>||</code> expressions. So, generally, coercion is very helpful in this case.</p><h3 id="implicitly-boolean" tabindex="-1"><a class="header-anchor" href="#implicitly-boolean" aria-hidden="true">#</a> Implicitly: * --&gt; Boolean</h3><p>Now, let&#39;s turn our attention to <em>implicit</em> coercion to <code>boolean</code> values, as it&#39;s by far the most common and also by far the most potentially troublesome.</p><p>Remember, <em>implicit</em> coercion is what kicks in when you use a value in such a way that it forces the value to be converted. For numeric and <code>string</code> operations, it&#39;s fairly easy to see how the coercions can occur.</p><p>But, what sort of expression operations require/force (<em>implicitly</em>) a <code>boolean</code> coercion?</p><ol><li>The test expression in an <code>if (..)</code> statement.</li><li>The test expression (second clause) in a <code>for ( .. ; .. ; .. )</code> header.</li><li>The test expression in <code>while (..)</code> and <code>do..while(..)</code> loops.</li><li>The test expression (first clause) in <code>? :</code> ternary expressions.</li><li>The left-hand operand (which serves as a test expression -- see below!) to the <code>||</code> (&quot;logical or&quot;) and <code>&amp;&amp;</code> (&quot;logical and&quot;) operators.</li></ol><p>Any value used in these contexts that is not already a <code>boolean</code> will be <em>implicitly</em> coerced to a <code>boolean</code> using the rules of the <code>ToBoolean</code> abstract operation covered earlier in this chapter.</p><p>Let&#39;s look at some examples:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token string">&quot;abc&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> c<span class="token punctuation">;</span>
<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>a<span class="token punctuation">)</span> <span class="token punctuation">{</span>
	console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> <span class="token string">&quot;yep&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// yep</span>
<span class="token punctuation">}</span>

<span class="token keyword">while</span> <span class="token punctuation">(</span>c<span class="token punctuation">)</span> <span class="token punctuation">{</span>
	console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> <span class="token string">&quot;nope, never runs&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

c <span class="token operator">=</span> d <span class="token operator">?</span> a <span class="token operator">:</span> b<span class="token punctuation">;</span>
c<span class="token punctuation">;</span>					<span class="token comment">// &quot;abc&quot;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token punctuation">(</span>a <span class="token operator">&amp;&amp;</span> d<span class="token punctuation">)</span> <span class="token operator">||</span> c<span class="token punctuation">)</span> <span class="token punctuation">{</span>
	console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> <span class="token string">&quot;yep&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>		<span class="token comment">// yep</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>In all these contexts, the non-<code>boolean</code> values are <em>implicitly coerced</em> to their <code>boolean</code> equivalents to make the test decisions.</p><h3 id="operators-and" tabindex="-1"><a class="header-anchor" href="#operators-and" aria-hidden="true">#</a> Operators <code>||</code> and <code>&amp;&amp;</code></h3><p>It&#39;s quite likely that you have seen the <code>||</code> (&quot;logical or&quot;) and <code>&amp;&amp;</code> (&quot;logical and&quot;) operators in most or all other languages you&#39;ve used. So it&#39;d be natural to assume that they work basically the same in JavaScript as in other similar languages.</p><p>There&#39;s some very little known, but very important, nuance here.</p><p>In fact, I would argue these operators shouldn&#39;t even be called &quot;logical ___ operators&quot;, as that name is incomplete in describing what they do. If I were to give them a more accurate (if more clumsy) name, I&#39;d call them &quot;selector operators,&quot; or more completely, &quot;operand selector operators.&quot;</p><p>Why? Because they don&#39;t actually result in a <em>logic</em> value (aka <code>boolean</code>) in JavaScript, as they do in some other languages.</p><p>So what <em>do</em> they result in? They result in the value of one (and only one) of their two operands. In other words, <strong>they select one of the two operand&#39;s values</strong>.</p><p>Quoting the ES5 spec from section 11.11:</p><blockquote><p>The value produced by a &amp;&amp; or || operator is not necessarily of type Boolean. The value produced will always be the value of one of the two operand expressions.</p></blockquote><p>Let&#39;s illustrate:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token string">&quot;abc&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>

a <span class="token operator">||</span> b<span class="token punctuation">;</span>		<span class="token comment">// 42</span>
a <span class="token operator">&amp;&amp;</span> b<span class="token punctuation">;</span>		<span class="token comment">// &quot;abc&quot;</span>

c <span class="token operator">||</span> b<span class="token punctuation">;</span>		<span class="token comment">// &quot;abc&quot;</span>
c <span class="token operator">&amp;&amp;</span> b<span class="token punctuation">;</span>		<span class="token comment">// null</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><strong>Wait, what!?</strong> Think about that. In languages like C and PHP, those expressions result in <code>true</code> or <code>false</code>, but in JS (and Python and Ruby, for that matter!), the result comes from the values themselves.</p><p>Both <code>||</code> and <code>&amp;&amp;</code> operators perform a <code>boolean</code> test on the <strong>first operand</strong> (<code>a</code> or <code>c</code>). If the operand is not already <code>boolean</code> (as it&#39;s not, here), a normal <code>ToBoolean</code> coercion occurs, so that the test can be performed.</p><p>For the <code>||</code> operator, if the test is <code>true</code>, the <code>||</code> expression results in the value of the <em>first operand</em> (<code>a</code> or <code>c</code>). If the test is <code>false</code>, the <code>||</code> expression results in the value of the <em>second operand</em> (<code>b</code>).</p><p>Inversely, for the <code>&amp;&amp;</code> operator, if the test is <code>true</code>, the <code>&amp;&amp;</code> expression results in the value of the <em>second operand</em> (<code>b</code>). If the test is <code>false</code>, the <code>&amp;&amp;</code> expression results in the value of the <em>first operand</em> (<code>a</code> or <code>c</code>).</p><p>The result of a <code>||</code> or <code>&amp;&amp;</code> expression is always the underlying value of one of the operands, <strong>not</strong> the (possibly coerced) result of the test. In <code>c &amp;&amp; b</code>, <code>c</code> is <code>null</code>, and thus falsy. But the <code>&amp;&amp;</code> expression itself results in <code>null</code> (the value in <code>c</code>), not in the coerced <code>false</code> used in the test.</p><p>Do you see how these operators act as &quot;operand selectors&quot;, now?</p><p>Another way of thinking about these operators:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code>a <span class="token operator">||</span> b<span class="token punctuation">;</span>
<span class="token comment">// roughly equivalent to:</span>
a <span class="token operator">?</span> a <span class="token operator">:</span> b<span class="token punctuation">;</span>

a <span class="token operator">&amp;&amp;</span> b<span class="token punctuation">;</span>
<span class="token comment">// roughly equivalent to:</span>
a <span class="token operator">?</span> b <span class="token operator">:</span> a<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><strong>Note:</strong> I call <code>a || b</code> &quot;roughly equivalent&quot; to <code>a ? a : b</code> because the outcome is identical, but there&#39;s a nuanced difference. In <code>a ? a : b</code>, if <code>a</code> was a more complex expression (like for instance one that might have side effects like calling a <code>function</code>, etc.), then the <code>a</code> expression would possibly be evaluated twice (if the first evaluation was truthy). By contrast, for <code>a || b</code>, the <code>a</code> expression is evaluated only once, and that value is used both for the coercive test as well as the result value (if appropriate). The same nuance applies to the <code>a &amp;&amp; b</code> and <code>a ? b : a</code> expressions.</p><p>An extremely common and helpful usage of this behavior, which there&#39;s a good chance you may have used before and not fully understood, is:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">function</span> <span class="token function">foo</span><span class="token punctuation">(</span><span class="token parameter">a<span class="token punctuation">,</span>b</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	a <span class="token operator">=</span> a <span class="token operator">||</span> <span class="token string">&quot;hello&quot;</span><span class="token punctuation">;</span>
	b <span class="token operator">=</span> b <span class="token operator">||</span> <span class="token string">&quot;world&quot;</span><span class="token punctuation">;</span>

	console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> a <span class="token operator">+</span> <span class="token string">&quot; &quot;</span> <span class="token operator">+</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token function">foo</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>					<span class="token comment">// &quot;hello world&quot;</span>
<span class="token function">foo</span><span class="token punctuation">(</span> <span class="token string">&quot;yeah&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;yeah!&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// &quot;yeah yeah!&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>The <code>a = a || &quot;hello&quot;</code> idiom (sometimes said to be JavaScript&#39;s version of the C# &quot;null coalescing operator&quot;) acts to test <code>a</code> and if it has no value (or only an undesired falsy value), provides a backup default value (<code>&quot;hello&quot;</code>).</p><p><strong>Be careful</strong>, though!</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token function">foo</span><span class="token punctuation">(</span> <span class="token string">&quot;That&#39;s it!&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;That&#39;s it! world&quot; &lt;-- Oops!</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>See the problem? <code>&quot;&quot;</code> as the second argument is a falsy value (see <code>ToBoolean</code> earlier in this chapter), so the <code>b = b || &quot;world&quot;</code> test fails, and the <code>&quot;world&quot;</code> default value is substituted, even though the intent probably was to have the explicitly passed <code>&quot;&quot;</code> be the value assigned to <code>b</code>.</p><p>This <code>||</code> idiom is extremely common, and quite helpful, but you have to use it only in cases where <em>all falsy values</em> should be skipped. Otherwise, you&#39;ll need to be more explicit in your test, and probably use a <code>? :</code> ternary instead.</p><p>This <em>default value assignment</em> idiom is so common (and useful!) that even those who publicly and vehemently decry JavaScript coercion often use it in their own code!</p><p>What about <code>&amp;&amp;</code>?</p><p>There&#39;s another idiom that is quite a bit less commonly authored manually, but which is used by JS minifiers frequently. The <code>&amp;&amp;</code> operator &quot;selects&quot; the second operand if and only if the first operand tests as truthy, and this usage is sometimes called the &quot;guard operator&quot; (also see &quot;Short Circuited&quot; in Chapter 5) -- the first expression test &quot;guards&quot; the second expression:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">function</span> <span class="token function">foo</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>

a <span class="token operator">&amp;&amp;</span> <span class="token function">foo</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 42</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><code>foo()</code> gets called only because <code>a</code> tests as truthy. If that test failed, this <code>a &amp;&amp; foo()</code> expression statement would just silently stop -- this is known as &quot;short circuiting&quot; -- and never call <code>foo()</code>.</p><p>Again, it&#39;s not nearly as common for people to author such things. Usually, they&#39;d do <code>if (a) { foo(); }</code> instead. But JS minifiers choose <code>a &amp;&amp; foo()</code> because it&#39;s much shorter. So, now, if you ever have to decipher such code, you&#39;ll know what it&#39;s doing and why.</p><p>OK, so <code>||</code> and <code>&amp;&amp;</code> have some neat tricks up their sleeve, as long as you&#39;re willing to allow the <em>implicit</em> coercion into the mix.</p><p><strong>Note:</strong> Both the <code>a = b || &quot;something&quot;</code> and <code>a &amp;&amp; b()</code> idioms rely on short circuiting behavior, which we cover in more detail in Chapter 5.</p><p>The fact that these operators don&#39;t actually result in <code>true</code> and <code>false</code> is possibly messing with your head a little bit by now. You&#39;re probably wondering how all your <code>if</code> statements and <code>for</code> loops have been working, if they&#39;ve included compound logical expressions like <code>a &amp;&amp; (b || c)</code>.</p><p>Don&#39;t worry! The sky is not falling. Your code is (probably) just fine. It&#39;s just that you probably never realized before that there was an <em>implicit</em> coercion to <code>boolean</code> going on <strong>after</strong> the compound expression was evaluated.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token string">&quot;foo&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>a <span class="token operator">&amp;&amp;</span> <span class="token punctuation">(</span>b <span class="token operator">||</span> c<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> <span class="token string">&quot;yep&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>This code still works the way you always thought it did, except for one subtle extra detail. The <code>a &amp;&amp; (b || c)</code> expression <em>actually</em> results in <code>&quot;foo&quot;</code>, not <code>true</code>. So, the <code>if</code> statement <em>then</em> forces the <code>&quot;foo&quot;</code> value to coerce to a <code>boolean</code>, which of course will be <code>true</code>.</p><p>See? No reason to panic. Your code is probably still safe. But now you know more about how it does what it does.</p><p>And now you also realize that such code is using <em>implicit</em> coercion. If you&#39;re in the &quot;avoid (implicit) coercion camp&quot; still, you&#39;re going to need to go back and make all of those tests <em>explicit</em>:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span><span class="token operator">!</span>a <span class="token operator">&amp;&amp;</span> <span class="token punctuation">(</span><span class="token operator">!</span><span class="token operator">!</span>b <span class="token operator">||</span> <span class="token operator">!</span><span class="token operator">!</span>c<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> <span class="token string">&quot;yep&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Good luck with that! ... Sorry, just teasing.</p><h3 id="symbol-coercion" tabindex="-1"><a class="header-anchor" href="#symbol-coercion" aria-hidden="true">#</a> Symbol Coercion</h3><p>Up to this point, there&#39;s been almost no observable outcome difference between <em>explicit</em> and <em>implicit</em> coercion -- only the readability of code has been at stake.</p><p>But ES6 Symbols introduce a gotcha into the coercion system that we need to discuss briefly. For reasons that go well beyond the scope of what we&#39;ll discuss in this book, <em>explicit</em> coercion of a <code>symbol</code> to a <code>string</code> is allowed, but <em>implicit</em> coercion of the same is disallowed and throws an error.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> s1 <span class="token operator">=</span> <span class="token function">Symbol</span><span class="token punctuation">(</span> <span class="token string">&quot;cool&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token function">String</span><span class="token punctuation">(</span> s1 <span class="token punctuation">)</span><span class="token punctuation">;</span>					<span class="token comment">// &quot;Symbol(cool)&quot;</span>

<span class="token keyword">var</span> s2 <span class="token operator">=</span> <span class="token function">Symbol</span><span class="token punctuation">(</span> <span class="token string">&quot;not cool&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
s2 <span class="token operator">+</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>						<span class="token comment">// TypeError</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><code>symbol</code> values cannot coerce to <code>number</code> at all (throws an error either way), but strangely they can both <em>explicitly</em> and <em>implicitly</em> coerce to <code>boolean</code> (always <code>true</code>).</p><p>Consistency is always easier to learn, and exceptions are never fun to deal with, but we just need to be careful around the new ES6 <code>symbol</code> values and how we coerce them.</p><p>The good news: it&#39;s probably going to be exceedingly rare for you to need to coerce a <code>symbol</code> value. The way they&#39;re typically used (see Chapter 3) will probably not call for coercion on a normal basis.</p><h2 id="loose-equals-vs-strict-equals" tabindex="-1"><a class="header-anchor" href="#loose-equals-vs-strict-equals" aria-hidden="true">#</a> Loose Equals vs. Strict Equals</h2><p>Loose equals is the <code>==</code> operator, and strict equals is the <code>===</code> operator. Both operators are used for comparing two values for &quot;equality,&quot; but the &quot;loose&quot; vs. &quot;strict&quot; indicates a <strong>very important</strong> difference in behavior between the two, specifically in how they decide &quot;equality.&quot;</p><p>A very common misconception about these two operators is: &quot;<code>==</code> checks values for equality and <code>===</code> checks both values and types for equality.&quot; While that sounds nice and reasonable, it&#39;s inaccurate. Countless well-respected JavaScript books and blogs have said exactly that, but unfortunately they&#39;re all <em>wrong</em>.</p><p>The correct description is: &quot;<code>==</code> allows coercion in the equality comparison and <code>===</code> disallows coercion.&quot;</p><h3 id="equality-performance" tabindex="-1"><a class="header-anchor" href="#equality-performance" aria-hidden="true">#</a> Equality Performance</h3><p>Stop and think about the difference between the first (inaccurate) explanation and this second (accurate) one.</p><p>In the first explanation, it seems obvious that <code>===</code> is <em>doing more work</em> than <code>==</code>, because it has to <em>also</em> check the type. In the second explanation, <code>==</code> is the one <em>doing more work</em> because it has to follow through the steps of coercion if the types are different.</p><p>Don&#39;t fall into the trap, as many have, of thinking this has anything to do with performance, though, as if <code>==</code> is going to be slower than <code>===</code> in any relevant way. While it&#39;s measurable that coercion does take <em>a little bit</em> of processing time, it&#39;s mere microseconds (yes, that&#39;s millionths of a second!).</p><p>If you&#39;re comparing two values of the same types, <code>==</code> and <code>===</code> use the identical algorithm, and so other than minor differences in engine implementation, they should do the same work.</p><p>If you&#39;re comparing two values of different types, the performance isn&#39;t the important factor. What you should be asking yourself is: when comparing these two values, do I want coercion or not?</p><p>If you want coercion, use <code>==</code> loose equality, but if you don&#39;t want coercion, use <code>===</code> strict equality.</p><p><strong>Note:</strong> The implication here then is that both <code>==</code> and <code>===</code> check the types of their operands. The difference is in how they respond if the types don&#39;t match.</p><h3 id="abstract-equality" tabindex="-1"><a class="header-anchor" href="#abstract-equality" aria-hidden="true">#</a> Abstract Equality</h3><p>The <code>==</code> operator&#39;s behavior is defined as &quot;The Abstract Equality Comparison Algorithm&quot; in section 11.9.3 of the ES5 spec. What&#39;s listed there is a comprehensive but simple algorithm that explicitly states every possible combination of types, and how the coercions (if necessary) should happen for each combination.</p><p><strong>Warning:</strong> When (<em>implicit</em>) coercion is maligned as being too complicated and too flawed to be a <em>useful good part</em>, it is these rules of &quot;abstract equality&quot; that are being condemned. Generally, they are said to be too complex and too unintuitive for developers to practically learn and use, and that they are prone more to causing bugs in JS programs than to enabling greater code readability. I believe this is a flawed premise -- that you readers are competent developers who write (and read and understand!) algorithms (aka code) all day long. So, what follows is a plain exposition of the &quot;abstract equality&quot; in simple terms. But I implore you to also read the ES5 spec section 11.9.3. I think you&#39;ll be surprised at just how reasonable it is.</p><p>Basically, the first clause (11.9.3.1) says, if the two values being compared are of the same type, they are simply and naturally compared via Identity as you&#39;d expect. For example, <code>42</code> is only equal to <code>42</code>, and <code>&quot;abc&quot;</code> is only equal to <code>&quot;abc&quot;</code>.</p><p>Some minor exceptions to normal expectation to be aware of:</p><ul><li><code>NaN</code> is never equal to itself (see Chapter 2)</li><li><code>+0</code> and <code>-0</code> are equal to each other (see Chapter 2)</li></ul><p>The final provision in clause 11.9.3.1 is for <code>==</code> loose equality comparison with <code>object</code>s (including <code>function</code>s and <code>array</code>s). Two such values are only <em>equal</em> if they are both references to <em>the exact same value</em>. No coercion occurs here.</p><p><strong>Note:</strong> The <code>===</code> strict equality comparison is defined identically to 11.9.3.1, including the provision about two <code>object</code> values. It&#39;s a very little known fact that <strong><code>==</code> and <code>===</code> behave identically</strong> in the case where two <code>object</code>s are being compared!</p><p>The rest of the algorithm in 11.9.3 specifies that if you use <code>==</code> loose equality to compare two values of different types, one or both of the values will need to be <em>implicitly</em> coerced. This coercion happens so that both values eventually end up as the same type, which can then directly be compared for equality using simple value Identity.</p><p><strong>Note:</strong> The <code>!=</code> loose not-equality operation is defined exactly as you&#39;d expect, in that it&#39;s literally the <code>==</code> operation comparison performed in its entirety, then the negation of the result. The same goes for the <code>!==</code> strict not-equality operation.</p><h4 id="comparing-strings-to-numbers" tabindex="-1"><a class="header-anchor" href="#comparing-strings-to-numbers" aria-hidden="true">#</a> Comparing: <code>string</code>s to <code>number</code>s</h4><p>To illustrate <code>==</code> coercion, let&#39;s first build off the <code>string</code> and <code>number</code> examples earlier in this chapter:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>

a <span class="token operator">===</span> b<span class="token punctuation">;</span>	<span class="token comment">// false</span>
a <span class="token operator">==</span> b<span class="token punctuation">;</span>		<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>As we&#39;d expect, <code>a === b</code> fails, because no coercion is allowed, and indeed the <code>42</code> and <code>&quot;42&quot;</code> values are different.</p><p>However, the second comparison <code>a == b</code> uses loose equality, which means that if the types happen to be different, the comparison algorithm will perform <em>implicit</em> coercion on one or both values.</p><p>But exactly what kind of coercion happens here? Does the <code>a</code> value of <code>42</code> become a <code>string</code>, or does the <code>b</code> value of <code>&quot;42&quot;</code> become a <code>number</code>?</p><p>In the ES5 spec, clauses 11.9.3.4-5 say:</p><blockquote><ol start="4"><li>If Type(x) is Number and Type(y) is String, return the result of the comparison x == ToNumber(y).</li><li>If Type(x) is String and Type(y) is Number, return the result of the comparison ToNumber(x) == y.</li></ol></blockquote><p><strong>Warning:</strong> The spec uses <code>Number</code> and <code>String</code> as the formal names for the types, while this book prefers <code>number</code> and <code>string</code> for the primitive types. Do not let the capitalization of <code>Number</code> in the spec confuse you for the <code>Number()</code> native function. For our purposes, the capitalization of the type name is irrelevant -- they have basically the same meaning.</p><p>Clearly, the spec says the <code>&quot;42&quot;</code> value is coerced to a <code>number</code> for the comparison. The <em>how</em> of that coercion has already been covered earlier, specifically with the <code>ToNumber</code> abstract operation. In this case, it&#39;s quite obvious then that the resulting two <code>42</code> values are equal.</p><h4 id="comparing-anything-to-boolean" tabindex="-1"><a class="header-anchor" href="#comparing-anything-to-boolean" aria-hidden="true">#</a> Comparing: anything to <code>boolean</code></h4><p>One of the biggest gotchas with the <em>implicit</em> coercion of <code>==</code> loose equality pops up when you try to compare a value directly to <code>true</code> or <code>false</code>.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>

a <span class="token operator">==</span> b<span class="token punctuation">;</span>	<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Wait, what happened here!? We know that <code>&quot;42&quot;</code> is a truthy value (see earlier in this chapter). So, how come it&#39;s not <code>==</code> loose equal to <code>true</code>?</p><p>The reason is both simple and deceptively tricky. It&#39;s so easy to misunderstand, many JS developers never pay close enough attention to fully grasp it.</p><p>Let&#39;s again quote the spec, clauses 11.9.3.6-7:</p><blockquote><ol start="6"><li>If Type(x) is Boolean, return the result of the comparison ToNumber(x) == y.</li><li>If Type(y) is Boolean, return the result of the comparison x == ToNumber(y).</li></ol></blockquote><p>Let&#39;s break that down. First:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> x <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> y <span class="token operator">=</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>

x <span class="token operator">==</span> y<span class="token punctuation">;</span> <span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The <code>Type(x)</code> is indeed <code>Boolean</code>, so it performs <code>ToNumber(x)</code>, which coerces <code>true</code> to <code>1</code>. Now, <code>1 == &quot;42&quot;</code> is evaluated. The types are still different, so (essentially recursively) we reconsult the algorithm, which just as above will coerce <code>&quot;42&quot;</code> to <code>42</code>, and <code>1 == 42</code> is clearly <code>false</code>.</p><p>Reverse it, and we still get the same outcome:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> x <span class="token operator">=</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> y <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span>

x <span class="token operator">==</span> y<span class="token punctuation">;</span> <span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The <code>Type(y)</code> is <code>Boolean</code> this time, so <code>ToNumber(y)</code> yields <code>0</code>. <code>&quot;42&quot; == 0</code> recursively becomes <code>42 == 0</code>, which is of course <code>false</code>.</p><p>In other words, <strong>the value <code>&quot;42&quot;</code> is neither <code>== true</code> nor <code>== false</code>.</strong> At first, that statement might seem crazy. How can a value be neither truthy nor falsy?</p><p>But that&#39;s the problem! You&#39;re asking the wrong question, entirely. It&#39;s not your fault, really. Your brain is tricking you.</p><p><code>&quot;42&quot;</code> is indeed truthy, but <code>&quot;42&quot; == true</code> <strong>is not performing a boolean test/coercion</strong> at all, no matter what your brain says. <code>&quot;42&quot;</code> <em>is not</em> being coerced to a <code>boolean</code> (<code>true</code>), but instead <code>true</code> is being coerced to a <code>1</code>, and then <code>&quot;42&quot;</code> is being coerced to <code>42</code>.</p><p>Whether we like it or not, <code>ToBoolean</code> is not even involved here, so the truthiness or falsiness of <code>&quot;42&quot;</code> is irrelevant to the <code>==</code> operation!</p><p>What <em>is</em> relevant is to understand how the <code>==</code> comparison algorithm behaves with all the different type combinations. As it regards a <code>boolean</code> value on either side of the <code>==</code>, a <code>boolean</code> always coerces to a <code>number</code> <em>first</em>.</p><p>If that seems strange to you, you&#39;re not alone. I personally would recommend to never, ever, under any circumstances, use <code>== true</code> or <code>== false</code>. Ever.</p><p>But remember, I&#39;m only talking about <code>==</code> here. <code>=== true</code> and <code>=== false</code> wouldn&#39;t allow the coercion, so they&#39;re safe from this hidden <code>ToNumber</code> coercion.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>

<span class="token comment">// bad (will fail!):</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>a <span class="token operator">==</span> <span class="token boolean">true</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token comment">// ..</span>
<span class="token punctuation">}</span>

<span class="token comment">// also bad (will fail!):</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>a <span class="token operator">===</span> <span class="token boolean">true</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token comment">// ..</span>
<span class="token punctuation">}</span>

<span class="token comment">// good enough (works implicitly):</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>a<span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token comment">// ..</span>
<span class="token punctuation">}</span>

<span class="token comment">// better (works explicitly):</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span><span class="token operator">!</span>a<span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token comment">// ..</span>
<span class="token punctuation">}</span>

<span class="token comment">// also great (works explicitly):</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token function">Boolean</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token comment">// ..</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br></div></div><p>If you avoid ever using <code>== true</code> or <code>== false</code> (aka loose equality with <code>boolean</code>s) in your code, you&#39;ll never have to worry about this truthiness/falsiness mental gotcha.</p><h4 id="comparing-nulls-to-undefineds" tabindex="-1"><a class="header-anchor" href="#comparing-nulls-to-undefineds" aria-hidden="true">#</a> Comparing: <code>null</code>s to <code>undefined</code>s</h4><p>Another example of <em>implicit</em> coercion can be seen with <code>==</code> loose equality between <code>null</code> and <code>undefined</code> values. Yet again quoting the ES5 spec, clauses 11.9.3.2-3:</p><blockquote><ol start="2"><li>If x is null and y is undefined, return true.</li><li>If x is undefined and y is null, return true.</li></ol></blockquote><p><code>null</code> and <code>undefined</code>, when compared with <code>==</code> loose equality, equate to (aka coerce to) each other (as well as themselves, obviously), and no other values in the entire language.</p><p>What this means is that <code>null</code> and <code>undefined</code> can be treated as indistinguishable for comparison purposes, if you use the <code>==</code> loose equality operator to allow their mutual <em>implicit</em> coercion.</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b<span class="token punctuation">;</span>

a <span class="token operator">==</span> b<span class="token punctuation">;</span>		<span class="token comment">// true</span>
a <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">;</span>	<span class="token comment">// true</span>
b <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">;</span>	<span class="token comment">// true</span>

a <span class="token operator">==</span> <span class="token boolean">false</span><span class="token punctuation">;</span>	<span class="token comment">// false</span>
b <span class="token operator">==</span> <span class="token boolean">false</span><span class="token punctuation">;</span>	<span class="token comment">// false</span>
a <span class="token operator">==</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>	<span class="token comment">// false</span>
b <span class="token operator">==</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>	<span class="token comment">// false</span>
a <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">;</span>		<span class="token comment">// false</span>
b <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">;</span>		<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>The coercion between <code>null</code> and <code>undefined</code> is safe and predictable, and no other values can give false positives in such a check. I recommend using this coercion to allow <code>null</code> and <code>undefined</code> to be indistinguishable and thus treated as the same value.</p><p>For example:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token function">doSomething</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>a <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token comment">// ..</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>The <code>a == null</code> check will pass only if <code>doSomething()</code> returns either <code>null</code> or <code>undefined</code>, and will fail with any other value, even other falsy values like <code>0</code>, <code>false</code>, and <code>&quot;&quot;</code>.</p><p>The <em>explicit</em> form of the check, which disallows any such coercion, is (I think) unnecessarily much uglier (and perhaps a tiny bit less performant!):</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token function">doSomething</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>a <span class="token operator">===</span> <span class="token keyword">undefined</span> <span class="token operator">||</span> a <span class="token operator">===</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token comment">// ..</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>In my opinion, the form <code>a == null</code> is yet another example where <em>implicit</em> coercion improves code readability, but does so in a reliably safe way.</p><h4 id="comparing-objects-to-non-objects" tabindex="-1"><a class="header-anchor" href="#comparing-objects-to-non-objects" aria-hidden="true">#</a> Comparing: <code>object</code>s to non-<code>object</code>s</h4><p>If an <code>object</code>/<code>function</code>/<code>array</code> is compared to a simple scalar primitive (<code>string</code>, <code>number</code>, or <code>boolean</code>), the ES5 spec says in clauses 11.9.3.8-9:</p><blockquote><ol start="8"><li>If Type(x) is either String or Number and Type(y) is Object, return the result of the comparison x == ToPrimitive(y).</li><li>If Type(x) is Object and Type(y) is either String or Number, return the result of the comparison ToPrimitive(x) == y.</li></ol></blockquote><p><strong>Note:</strong> You may notice that these clauses only mention <code>String</code> and <code>Number</code>, but not <code>Boolean</code>. That&#39;s because, as quoted earlier, clauses 11.9.3.6-7 take care of coercing any <code>Boolean</code> operand presented to a <code>Number</code> first.</p><p>Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token number">42</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">[</span> <span class="token number">42</span> <span class="token punctuation">]</span><span class="token punctuation">;</span>

a <span class="token operator">==</span> b<span class="token punctuation">;</span>	<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The <code>[ 42 ]</code> value has its <code>ToPrimitive</code> abstract operation called (see the &quot;Abstract Value Operations&quot; section earlier), which results in the <code>&quot;42&quot;</code> value. From there, it&#39;s just <code>42 == &quot;42&quot;</code>, which as we&#39;ve already covered becomes <code>42 == 42</code>, so <code>a</code> and <code>b</code> are found to be coercively equal.</p><p><strong>Tip:</strong> All the quirks of the <code>ToPrimitive</code> abstract operation that we discussed earlier in this chapter (<code>toString()</code>, <code>valueOf()</code>) apply here as you&#39;d expect. This can be quite useful if you have a complex data structure that you want to define a custom <code>valueOf()</code> method on, to provide a simple value for equality comparison purposes.</p><p>In Chapter 3, we covered &quot;unboxing,&quot; where an <code>object</code> wrapper around a primitive value (like from <code>new String(&quot;abc&quot;)</code>, for instance) is unwrapped, and the underlying primitive value (<code>&quot;abc&quot;</code>) is returned. This behavior is related to the <code>ToPrimitive</code> coercion in the <code>==</code> algorithm:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token string">&quot;abc&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token function">Object</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// same as \`new String( a )\`</span>

a <span class="token operator">===</span> b<span class="token punctuation">;</span>				<span class="token comment">// false</span>
a <span class="token operator">==</span> b<span class="token punctuation">;</span>					<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><code>a == b</code> is <code>true</code> because <code>b</code> is coerced (aka &quot;unboxed,&quot; unwrapped) via <code>ToPrimitive</code> to its underlying <code>&quot;abc&quot;</code> simple scalar primitive value, which is the same as the value in <code>a</code>.</p><p>There are some values where this is not the case, though, because of other overriding rules in the <code>==</code> algorithm. Consider:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token function">Object</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// same as \`Object()\`</span>
a <span class="token operator">==</span> b<span class="token punctuation">;</span>					<span class="token comment">// false</span>

<span class="token keyword">var</span> c <span class="token operator">=</span> <span class="token keyword">undefined</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> d <span class="token operator">=</span> <span class="token function">Object</span><span class="token punctuation">(</span> c <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// same as \`Object()\`</span>
c <span class="token operator">==</span> d<span class="token punctuation">;</span>					<span class="token comment">// false</span>

<span class="token keyword">var</span> e <span class="token operator">=</span> <span class="token number">NaN</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> f <span class="token operator">=</span> <span class="token function">Object</span><span class="token punctuation">(</span> e <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// same as \`new Number( e )\`</span>
e <span class="token operator">==</span> f<span class="token punctuation">;</span>					<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>The <code>null</code> and <code>undefined</code> values cannot be boxed -- they have no object wrapper equivalent -- so <code>Object(null)</code> is just like <code>Object()</code> in that both just produce a normal object.</p><p><code>NaN</code> can be boxed to its <code>Number</code> object wrapper equivalent, but when <code>==</code> causes an unboxing, the <code>NaN == NaN</code> comparison fails because <code>NaN</code> is never equal to itself (see Chapter 2).</p><h3 id="edge-cases" tabindex="-1"><a class="header-anchor" href="#edge-cases" aria-hidden="true">#</a> Edge Cases</h3><p>Now that we&#39;ve thoroughly examined how the <em>implicit</em> coercion of <code>==</code> loose equality works (in both sensible and surprising ways), let&#39;s try to call out the worst, craziest corner cases so we can see what we need to avoid to not get bitten with coercion bugs.</p><p>First, let&#39;s examine how modifying the built-in native prototypes can produce crazy results:</p><h4 id="a-number-by-any-other-value-would" tabindex="-1"><a class="header-anchor" href="#a-number-by-any-other-value-would" aria-hidden="true">#</a> A Number By Any Other Value Would...</h4><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token class-name">Number</span><span class="token punctuation">.</span>prototype<span class="token punctuation">.</span><span class="token function-variable function">valueOf</span> <span class="token operator">=</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token keyword">return</span> <span class="token number">3</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">new</span> <span class="token class-name">Number</span><span class="token punctuation">(</span> <span class="token number">2</span> <span class="token punctuation">)</span> <span class="token operator">==</span> <span class="token number">3</span><span class="token punctuation">;</span>	<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><strong>Warning:</strong> <code>2 == 3</code> would not have fallen into this trap, because neither <code>2</code> nor <code>3</code> would have invoked the built-in <code>Number.prototype.valueOf()</code> method because both are already primitive <code>number</code> values and can be compared directly. However, <code>new Number(2)</code> must go through the <code>ToPrimitive</code> coercion, and thus invoke <code>valueOf()</code>.</p><p>Evil, huh? Of course it is. No one should ever do such a thing. The fact that you <em>can</em> do this is sometimes used as a criticism of coercion and <code>==</code>. But that&#39;s misdirected frustration. JavaScript is not <em>bad</em> because you can do such things, a developer is <em>bad</em> <strong>if they do such things</strong>. Don&#39;t fall into the &quot;my programming language should protect me from myself&quot; fallacy.</p><p>Next, let&#39;s consider another tricky example, which takes the evil from the previous example to another level:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">if</span> <span class="token punctuation">(</span>a <span class="token operator">==</span> <span class="token number">2</span> <span class="token operator">&amp;&amp;</span> a <span class="token operator">==</span> <span class="token number">3</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token comment">// ..</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>You might think this would be impossible, because <code>a</code> could never be equal to both <code>2</code> and <code>3</code> <em>at the same time</em>. But &quot;at the same time&quot; is inaccurate, since the first expression <code>a == 2</code> happens strictly <em>before</em> <code>a == 3</code>.</p><p>So, what if we make <code>a.valueOf()</code> have side effects each time it&#39;s called, such that the first time it returns <code>2</code> and the second time it&#39;s called it returns <code>3</code>? Pretty easy:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> i <span class="token operator">=</span> <span class="token number">2</span><span class="token punctuation">;</span>

<span class="token class-name">Number</span><span class="token punctuation">.</span>prototype<span class="token punctuation">.</span><span class="token function-variable function">valueOf</span> <span class="token operator">=</span> <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token keyword">return</span> i<span class="token operator">++</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Number</span><span class="token punctuation">(</span> <span class="token number">42</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>a <span class="token operator">==</span> <span class="token number">2</span> <span class="token operator">&amp;&amp;</span> a <span class="token operator">==</span> <span class="token number">3</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> <span class="token string">&quot;Yep, this happened.&quot;</span> <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>Again, these are evil tricks. Don&#39;t do them. But also don&#39;t use them as complaints against coercion. Potential abuses of a mechanism are not sufficient evidence to condemn the mechanism. Just avoid these crazy tricks, and stick only with valid and proper usage of coercion.</p><h4 id="false-y-comparisons" tabindex="-1"><a class="header-anchor" href="#false-y-comparisons" aria-hidden="true">#</a> False-y Comparisons</h4><p>The most common complaint against <em>implicit</em> coercion in <code>==</code> comparisons comes from how falsy values behave surprisingly when compared to each other.</p><p>To illustrate, let&#39;s look at a list of the corner-cases around falsy value comparisons, to see which ones are reasonable and which are troublesome:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token string">&quot;0&quot;</span> <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">;</span>			<span class="token comment">// false</span>
<span class="token string">&quot;0&quot;</span> <span class="token operator">==</span> <span class="token keyword">undefined</span><span class="token punctuation">;</span>		<span class="token comment">// false</span>
<span class="token string">&quot;0&quot;</span> <span class="token operator">==</span> <span class="token boolean">false</span><span class="token punctuation">;</span>			<span class="token comment">// true -- UH OH!</span>
<span class="token string">&quot;0&quot;</span> <span class="token operator">==</span> <span class="token number">NaN</span><span class="token punctuation">;</span>				<span class="token comment">// false</span>
<span class="token string">&quot;0&quot;</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">;</span>				<span class="token comment">// true</span>
<span class="token string">&quot;0&quot;</span> <span class="token operator">==</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>				<span class="token comment">// false</span>

<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">;</span>			<span class="token comment">// false</span>
<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token keyword">undefined</span><span class="token punctuation">;</span>		<span class="token comment">// false</span>
<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token number">NaN</span><span class="token punctuation">;</span>			<span class="token comment">// false</span>
<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>			<span class="token comment">// true -- UH OH!</span>
<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>			<span class="token comment">// true -- UH OH!</span>
<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">;</span>			<span class="token comment">// false</span>

<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">;</span>				<span class="token comment">// false</span>
<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token keyword">undefined</span><span class="token punctuation">;</span>		<span class="token comment">// false</span>
<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token number">NaN</span><span class="token punctuation">;</span>				<span class="token comment">// false</span>
<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">;</span>				<span class="token comment">// false</span>

<span class="token number">0</span> <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">;</span>				<span class="token comment">// false</span>
<span class="token number">0</span> <span class="token operator">==</span> <span class="token keyword">undefined</span><span class="token punctuation">;</span>			<span class="token comment">// false</span>
<span class="token number">0</span> <span class="token operator">==</span> <span class="token number">NaN</span><span class="token punctuation">;</span>				<span class="token comment">// false</span>
<span class="token number">0</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
<span class="token number">0</span> <span class="token operator">==</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">;</span>				<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br><span class="line-number">27</span><br></div></div><p>In this list of 24 comparisons, 17 of them are quite reasonable and predictable. For example, we know that <code>&quot;&quot;</code> and <code>NaN</code> are not at all equatable values, and indeed they don&#39;t coerce to be loose equals, whereas <code>&quot;0&quot;</code> and <code>0</code> are reasonably equatable and <em>do</em> coerce as loose equals.</p><p>However, seven of the comparisons are marked with &quot;UH OH!&quot; because as false positives, they are much more likely gotchas that could trip you up. <code>&quot;&quot;</code> and <code>0</code> are definitely distinctly different values, and it&#39;s rare you&#39;d want to treat them as equatable, so their mutual coercion is troublesome. Note that there aren&#39;t any false negatives here.</p><h4 id="the-crazy-ones" tabindex="-1"><a class="header-anchor" href="#the-crazy-ones" aria-hidden="true">#</a> The Crazy Ones</h4><p>We don&#39;t have to stop there, though. We can keep looking for even more troublesome coercions:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">==</span> <span class="token operator">!</span><span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>		<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Oooo, that seems at a higher level of crazy, right!? Your brain may likely trick you that you&#39;re comparing a truthy to a falsy value, so the <code>true</code> result is surprising, as we <em>know</em> a value can never be truthy and falsy at the same time!</p><p>But that&#39;s not what&#39;s actually happening. Let&#39;s break it down. What do we know about the <code>!</code> unary operator? It explicitly coerces to a <code>boolean</code> using the <code>ToBoolean</code> rules (and it also flips the parity). So before <code>[] == ![]</code> is even processed, it&#39;s actually already translated to <code>[] == false</code>. We already saw that form in our above list (<code>false == []</code>), so its surprise result is <em>not new</em> to us.</p><p>How about other corner cases?</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token number">2</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token number">2</span><span class="token punctuation">]</span><span class="token punctuation">;</span>		<span class="token comment">// true</span>
<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token keyword">null</span><span class="token punctuation">]</span><span class="token punctuation">;</span>	<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>As we said earlier in our <code>ToNumber</code> discussion, the right-hand side <code>[2]</code> and <code>[null]</code> values will go through a <code>ToPrimitive</code> coercion so they can be more readily compared to the simple primitives (<code>2</code> and <code>&quot;&quot;</code>, respectively) on the left-hand side. Since the <code>valueOf()</code> for <code>array</code> values just returns the <code>array</code> itself, coercion falls to stringifying the <code>array</code>.</p><p><code>[2]</code> will become <code>&quot;2&quot;</code>, which then is <code>ToNumber</code> coerced to <code>2</code> for the right-hand side value in the first comparison. <code>[null]</code> just straight becomes <code>&quot;&quot;</code>.</p><p>So, <code>2 == 2</code> and <code>&quot;&quot; == &quot;&quot;</code> are completely understandable.</p><p>If your instinct is to still dislike these results, your frustration is not actually with coercion like you probably think it is. It&#39;s actually a complaint against the default <code>array</code> values&#39; <code>ToPrimitive</code> behavior of coercing to a <code>string</code> value. More likely, you&#39;d just wish that <code>[2].toString()</code> didn&#39;t return <code>&quot;2&quot;</code>, or that <code>[null].toString()</code> didn&#39;t return <code>&quot;&quot;</code>.</p><p>But what exactly <em>should</em> these <code>string</code> coercions result in? I can&#39;t really think of any other appropriate <code>string</code> coercion of <code>[2]</code> than <code>&quot;2&quot;</code>, except perhaps <code>&quot;[2]&quot;</code> -- but that could be very strange in other contexts!</p><p>You could rightly make the case that since <code>String(null)</code> becomes <code>&quot;null&quot;</code>, then <code>String([null])</code> should also become <code>&quot;null&quot;</code>. That&#39;s a reasonable assertion. So, that&#39;s the real culprit.</p><p><em>Implicit</em> coercion itself isn&#39;t the evil here. Even an <em>explicit</em> coercion of <code>[null]</code> to a <code>string</code> results in <code>&quot;&quot;</code>. What&#39;s at odds is whether it&#39;s sensible at all for <code>array</code> values to stringify to the equivalent of their contents, and exactly how that happens. So, direct your frustration at the rules for <code>String( [..] )</code>, because that&#39;s where the craziness stems from. Perhaps there should be no stringification coercion of <code>array</code>s at all? But that would have lots of other downsides in other parts of the language.</p><p>Another famously cited gotcha:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token number">0</span> <span class="token operator">==</span> <span class="token string">&quot;\\n&quot;</span><span class="token punctuation">;</span>		<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>As we discussed earlier with empty <code>&quot;&quot;</code>, <code>&quot;\\n&quot;</code> (or <code>&quot; &quot;</code> or any other whitespace combination) is coerced via <code>ToNumber</code>, and the result is <code>0</code>. What other <code>number</code> value would you expect whitespace to coerce to? Does it bother you that <em>explicit</em> <code>Number(&quot; &quot;)</code> yields <code>0</code>?</p><p>Really the only other reasonable <code>number</code> value that empty strings or whitespace strings could coerce to is the <code>NaN</code>. But would that <em>really</em> be better? The comparison <code>&quot; &quot; == NaN</code> would of course fail, but it&#39;s unclear that we&#39;d have really <em>fixed</em> any of the underlying concerns.</p><p>The chances that a real-world JS program fails because <code>0 == &quot;\\n&quot;</code> are awfully rare, and such corner cases are easy to avoid.</p><p>Type conversions <strong>always</strong> have corner cases, in any language -- nothing specific to coercion. The issues here are about second-guessing a certain set of corner cases (and perhaps rightly so!?), but that&#39;s not a salient argument against the overall coercion mechanism.</p><p>Bottom line: almost any crazy coercion between <em>normal values</em> that you&#39;re likely to run into (aside from intentionally tricky <code>valueOf()</code> or <code>toString()</code> hacks as earlier) will boil down to the short seven-item list of gotcha coercions we&#39;ve identified above.</p><p>To contrast against these 24 likely suspects for coercion gotchas, consider another list like this:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token number">42</span> <span class="token operator">==</span> <span class="token string">&quot;43&quot;</span><span class="token punctuation">;</span>							<span class="token comment">// false</span>
<span class="token string">&quot;foo&quot;</span> <span class="token operator">==</span> <span class="token number">42</span><span class="token punctuation">;</span>						<span class="token comment">// false</span>
<span class="token string">&quot;true&quot;</span> <span class="token operator">==</span> <span class="token boolean">true</span><span class="token punctuation">;</span>						<span class="token comment">// false</span>

<span class="token number">42</span> <span class="token operator">==</span> <span class="token string">&quot;42&quot;</span><span class="token punctuation">;</span>							<span class="token comment">// true</span>
<span class="token string">&quot;foo&quot;</span> <span class="token operator">==</span> <span class="token punctuation">[</span> <span class="token string">&quot;foo&quot;</span> <span class="token punctuation">]</span><span class="token punctuation">;</span>					<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>In these nonfalsy, noncorner cases (and there are literally an infinite number of comparisons we could put on this list), the coercion results are totally safe, reasonable, and explainable.</p><h4 id="sanity-check" tabindex="-1"><a class="header-anchor" href="#sanity-check" aria-hidden="true">#</a> Sanity Check</h4><p>OK, we&#39;ve definitely found some crazy stuff when we&#39;ve looked deeply into <em>implicit</em> coercion. No wonder that most developers claim coercion is evil and should be avoided, right!?</p><p>But let&#39;s take a step back and do a sanity check.</p><p>By way of magnitude comparison, we have <em>a list</em> of seven troublesome gotcha coercions, but we have <em>another list</em> of (at least 17, but actually infinite) coercions that are totally sane and explainable.</p><p>If you&#39;re looking for a textbook example of &quot;throwing the baby out with the bathwater,&quot; this is it: discarding the entirety of coercion (the infinitely large list of safe and useful behaviors) because of a list of literally just seven gotchas.</p><p>The more prudent reaction would be to ask, &quot;how can I use the countless <em>good parts</em> of coercion, but avoid the few <em>bad parts</em>?&quot;</p><p>Let&#39;s look again at the <em>bad</em> list:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token string">&quot;0&quot;</span> <span class="token operator">==</span> <span class="token boolean">false</span><span class="token punctuation">;</span>			<span class="token comment">// true -- UH OH!</span>
<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">;</span>			<span class="token comment">// true -- UH OH!</span>
<span class="token boolean">false</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>			<span class="token comment">// true -- UH OH!</span>
<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
<span class="token number">0</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Four of the seven items on this list involve <code>== false</code> comparison, which we said earlier you should <strong>always, always</strong> avoid. That&#39;s a pretty easy rule to remember.</p><p>Now the list is down to three.</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
<span class="token string">&quot;&quot;</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
<span class="token number">0</span> <span class="token operator">==</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>				<span class="token comment">// true -- UH OH!</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Are these reasonable coercions you&#39;d do in a normal JavaScript program? Under what conditions would they really happen?</p><p>I don&#39;t think it&#39;s terribly likely that you&#39;d literally use <code>== []</code> in a <code>boolean</code> test in your program, at least not if you know what you&#39;re doing. You&#39;d probably instead be doing <code>== &quot;&quot;</code> or <code>== 0</code>, like:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">function</span> <span class="token function">doSomething</span><span class="token punctuation">(</span><span class="token parameter">a</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token keyword">if</span> <span class="token punctuation">(</span>a <span class="token operator">==</span> <span class="token string">&quot;&quot;</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
		<span class="token comment">// ..</span>
	<span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>You&#39;d have an oops if you accidentally called <code>doSomething(0)</code> or <code>doSomething([])</code>. Another scenario:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">function</span> <span class="token function">doSomething</span><span class="token punctuation">(</span><span class="token parameter">a<span class="token punctuation">,</span>b</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
	<span class="token keyword">if</span> <span class="token punctuation">(</span>a <span class="token operator">==</span> b<span class="token punctuation">)</span> <span class="token punctuation">{</span>
		<span class="token comment">// ..</span>
	<span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Again, this could break if you did something like <code>doSomething(&quot;&quot;,0)</code> or <code>doSomething([],&quot;&quot;)</code>.</p><p>So, while the situations <em>can</em> exist where these coercions will bite you, and you&#39;ll want to be careful around them, they&#39;re probably not super common on the whole of your code base.</p><h4 id="safely-using-implicit-coercion" tabindex="-1"><a class="header-anchor" href="#safely-using-implicit-coercion" aria-hidden="true">#</a> Safely Using Implicit Coercion</h4><p>The most important advice I can give you: examine your program and reason about what values can show up on either side of an <code>==</code> comparison. To effectively avoid issues with such comparisons, here&#39;s some heuristic rules to follow:</p><ol><li>If either side of the comparison can have <code>true</code> or <code>false</code> values, don&#39;t ever, EVER use <code>==</code>.</li><li>If either side of the comparison can have <code>[]</code>, <code>&quot;&quot;</code>, or <code>0</code> values, seriously consider not using <code>==</code>.</li></ol><p>In these scenarios, it&#39;s almost certainly better to use <code>===</code> instead of <code>==</code>, to avoid unwanted coercion. Follow those two simple rules and pretty much all the coercion gotchas that could reasonably hurt you will effectively be avoided.</p><p><strong>Being more explicit/verbose in these cases will save you from a lot of headaches.</strong></p><p>The question of <code>==</code> vs. <code>===</code> is really appropriately framed as: should you allow coercion for a comparison or not?</p><p>There&#39;s lots of cases where such coercion can be helpful, allowing you to more tersely express some comparison logic (like with <code>null</code> and <code>undefined</code>, for example).</p><p>In the overall scheme of things, there&#39;s relatively few cases where <em>implicit</em> coercion is truly dangerous. But in those places, for safety sake, definitely use <code>===</code>.</p><p><strong>Tip:</strong> Another place where coercion is guaranteed <em>not</em> to bite you is with the <code>typeof</code> operator. <code>typeof</code> is always going to return you one of seven strings (see Chapter 1), and none of them are the empty <code>&quot;&quot;</code> string. As such, there&#39;s no case where checking the type of some value is going to run afoul of <em>implicit</em> coercion. <code>typeof x == &quot;function&quot;</code> is 100% as safe and reliable as <code>typeof x === &quot;function&quot;</code>. Literally, the spec says the algorithm will be identical in this situation. So, don&#39;t just blindly use <code>===</code> everywhere simply because that&#39;s what your code tools tell you to do, or (worst of all) because you&#39;ve been told in some book to <strong>not think about it</strong>. You own the quality of your code.</p><p>Is <em>implicit</em> coercion evil and dangerous? In a few cases, yes, but overwhelmingly, no.</p><p>Be a responsible and mature developer. Learn how to use the power of coercion (both <em>explicit</em> and <em>implicit</em>) effectively and safely. And teach those around you to do the same.</p><p>Here&#39;s a handy table made by Alex Dorey (@dorey on GitHub) to visualize a variety of comparisons:</p>`,551),i=["src"],l=n(`<p>Source: https://github.com/dorey/JavaScript-Equality-Table</p><h2 id="abstract-relational-comparison" tabindex="-1"><a class="header-anchor" href="#abstract-relational-comparison" aria-hidden="true">#</a> Abstract Relational Comparison</h2><p>While this part of <em>implicit</em> coercion often gets a lot less attention, it&#39;s important nonetheless to think about what happens with <code>a &lt; b</code> comparisons (similar to how we just examined <code>a == b</code> in depth).</p><p>The &quot;Abstract Relational Comparison&quot; algorithm in ES5 section 11.8.5 essentially divides itself into two parts: what to do if the comparison involves both <code>string</code> values (second half), or anything else (first half).</p><p><strong>Note:</strong> The algorithm is only defined for <code>a &lt; b</code>. So, <code>a &gt; b</code> is handled as <code>b &lt; a</code>.</p><p>The algorithm first calls <code>ToPrimitive</code> coercion on both values, and if the return result of either call is not a <code>string</code>, then both values are coerced to <code>number</code> values using the <code>ToNumber</code> operation rules, and compared numerically.</p><p>For example:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">[</span> <span class="token number">42</span> <span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">[</span> <span class="token string">&quot;43&quot;</span> <span class="token punctuation">]</span><span class="token punctuation">;</span>

a <span class="token operator">&lt;</span> b<span class="token punctuation">;</span>	<span class="token comment">// true</span>
b <span class="token operator">&lt;</span> a<span class="token punctuation">;</span>	<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><strong>Note:</strong> Similar caveats for <code>-0</code> and <code>NaN</code> apply here as they did in the <code>==</code> algorithm discussed earlier.</p><p>However, if both values are <code>string</code>s for the <code>&lt;</code> comparison, simple lexicographic (natural alphabetic) comparison on the characters is performed:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">[</span> <span class="token string">&quot;42&quot;</span> <span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">[</span> <span class="token string">&quot;043&quot;</span> <span class="token punctuation">]</span><span class="token punctuation">;</span>

a <span class="token operator">&lt;</span> b<span class="token punctuation">;</span>	<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><code>a</code> and <code>b</code> are <em>not</em> coerced to <code>number</code>s, because both of them end up as <code>string</code>s after the <code>ToPrimitive</code> coercion on the two <code>array</code>s. So, <code>&quot;42&quot;</code> is compared character by character to <code>&quot;043&quot;</code>, starting with the first characters <code>&quot;4&quot;</code> and <code>&quot;0&quot;</code>, respectively. Since <code>&quot;0&quot;</code> is lexicographically <em>less than</em> than <code>&quot;4&quot;</code>, the comparison returns <code>false</code>.</p><p>The exact same behavior and reasoning goes for:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">[</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">2</span> <span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">[</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">3</span> <span class="token punctuation">]</span><span class="token punctuation">;</span>

a <span class="token operator">&lt;</span> b<span class="token punctuation">;</span>	<span class="token comment">// false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Here, <code>a</code> becomes <code>&quot;4,2&quot;</code> and <code>b</code> becomes <code>&quot;0,4,3&quot;</code>, and those lexicographically compare identically to the previous snippet.</p><p>What about:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">{</span> <span class="token literal-property property">b</span><span class="token operator">:</span> <span class="token number">42</span> <span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">{</span> <span class="token literal-property property">b</span><span class="token operator">:</span> <span class="token number">43</span> <span class="token punctuation">}</span><span class="token punctuation">;</span>

a <span class="token operator">&lt;</span> b<span class="token punctuation">;</span>	<span class="token comment">// ??</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><code>a &lt; b</code> is also <code>false</code>, because <code>a</code> becomes <code>[object Object]</code> and <code>b</code> becomes <code>[object Object]</code>, and so clearly <code>a</code> is not lexicographically less than <code>b</code>.</p><p>But strangely:</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">{</span> <span class="token literal-property property">b</span><span class="token operator">:</span> <span class="token number">42</span> <span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token punctuation">{</span> <span class="token literal-property property">b</span><span class="token operator">:</span> <span class="token number">43</span> <span class="token punctuation">}</span><span class="token punctuation">;</span>

a <span class="token operator">&lt;</span> b<span class="token punctuation">;</span>	<span class="token comment">// false</span>
a <span class="token operator">==</span> b<span class="token punctuation">;</span>	<span class="token comment">// false</span>
a <span class="token operator">&gt;</span> b<span class="token punctuation">;</span>	<span class="token comment">// false</span>

a <span class="token operator">&lt;=</span> b<span class="token punctuation">;</span>	<span class="token comment">// true</span>
a <span class="token operator">&gt;=</span> b<span class="token punctuation">;</span>	<span class="token comment">// true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>Why is <code>a == b</code> not <code>true</code>? They&#39;re the same <code>string</code> value (<code>&quot;[object Object]&quot;</code>), so it seems they should be equal, right? Nope. Recall the previous discussion about how <code>==</code> works with <code>object</code> references.</p><p>But then how are <code>a &lt;= b</code> and <code>a &gt;= b</code> resulting in <code>true</code>, if <code>a &lt; b</code> <strong>and</strong> <code>a == b</code> <strong>and</strong> <code>a &gt; b</code> are all <code>false</code>?</p><p>Because the spec says for <code>a &lt;= b</code>, it will actually evaluate <code>b &lt; a</code> first, and then negate that result. Since <code>b &lt; a</code> is <em>also</em> <code>false</code>, the result of <code>a &lt;= b</code> is <code>true</code>.</p><p>That&#39;s probably awfully contrary to how you might have explained what <code>&lt;=</code> does up to now, which would likely have been the literal: &quot;less than <em>or</em> equal to.&quot; JS more accurately considers <code>&lt;=</code> as &quot;not greater than&quot; (<code>!(a &gt; b)</code>, which JS treats as <code>!(b &lt; a)</code>). Moreover, <code>a &gt;= b</code> is explained by first considering it as <code>b &lt;= a</code>, and then applying the same reasoning.</p><p>Unfortunately, there is no &quot;strict relational comparison&quot; as there is for equality. In other words, there&#39;s no way to prevent <em>implicit</em> coercion from occurring with relational comparisons like <code>a &lt; b</code>, other than to ensure that <code>a</code> and <code>b</code> are of the same type explicitly before making the comparison.</p><p>Use the same reasoning from our earlier <code>==</code> vs. <code>===</code> sanity check discussion. If coercion is helpful and reasonably safe, like in a <code>42 &lt; &quot;43&quot;</code> comparison, <strong>use it</strong>. On the other hand, if you need to be safe about a relational comparison, <em>explicitly coerce</em> the values first, before using <code>&lt;</code> (or its counterparts).</p><div class="language-javascript ext-js line-numbers-mode"><pre class="language-javascript"><code><span class="token keyword">var</span> a <span class="token operator">=</span> <span class="token punctuation">[</span> <span class="token number">42</span> <span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">var</span> b <span class="token operator">=</span> <span class="token string">&quot;043&quot;</span><span class="token punctuation">;</span>

a <span class="token operator">&lt;</span> b<span class="token punctuation">;</span>						<span class="token comment">// false -- string comparison!</span>
<span class="token function">Number</span><span class="token punctuation">(</span> a <span class="token punctuation">)</span> <span class="token operator">&lt;</span> <span class="token function">Number</span><span class="token punctuation">(</span> b <span class="token punctuation">)</span><span class="token punctuation">;</span>	<span class="token comment">// true -- number comparison!</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h2 id="review" tabindex="-1"><a class="header-anchor" href="#review" aria-hidden="true">#</a> Review</h2><p>In this chapter, we turned our attention to how JavaScript type conversions happen, called <strong>coercion</strong>, which can be characterized as either <em>explicit</em> or <em>implicit</em>.</p><p>Coercion gets a bad rap, but it&#39;s actually quite useful in many cases. An important task for the responsible JS developer is to take the time to learn all the ins and outs of coercion to decide which parts will help improve their code, and which parts they really should avoid.</p><p><em>Explicit</em> coercion is code which is obvious that the intent is to convert a value from one type to another. The benefit is improvement in readability and maintainability of code by reducing confusion.</p><p><em>Implicit</em> coercion is coercion that is &quot;hidden&quot; as a side-effect of some other operation, where it&#39;s not as obvious that the type conversion will occur. While it may seem that <em>implicit</em> coercion is the opposite of <em>explicit</em> and is thus bad (and indeed, many think so!), actually <em>implicit</em> coercion is also about improving the readability of code.</p><p>Especially for <em>implicit</em>, coercion must be used responsibly and consciously. Know why you&#39;re writing the code you&#39;re writing, and how it works. Strive to write code that others will easily be able to learn from and understand as well.</p>`,33);function u(s,d){return e(),o(p,null,[r,t("img",{src:s.$withBase("/you-dont-know-js-v1/types-grammar/fig1.png"),width:"600"},null,8,i),l],64)}var b=a(c,[["render",u],["__file","ch4.html.vue"]]);export{b as default};
